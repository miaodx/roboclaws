KIMI_API_KEY=sk-xx
NV_API_KEY=nvapi-xx
MIMO_TP_KEY=tp-xx

# Optional per-demo coding-agent model overrides. These are passed as
# `--model` only by the roboclaws Codex / Claude Code demo launchers; they do
# not modify the system Codex or Claude Code default configuration.
# ROBOCLAWS_CODE_AGENT_MODEL=gpt-5.2
# ROBOCLAWS_CODEX_MODEL=gpt-5.2
# ROBOCLAWS_CLAUDE_MODEL=sonnet
