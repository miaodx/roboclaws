{
  "schema": "roboclaws_skill_manifest_v1",
  "name": "runtime-map-prior-conversion",
  "summary": "Convert offline robot navigation memory and scene-engine room partitions into Runtime Map Prior Snapshot or room-overlay artifacts.",
  "abstraction_level": "agent_skill",
  "role": "map_artifact_conversion",
  "status": "active",
  "mcp": {
    "profiles": ["household_world"],
    "required_tools": [],
    "optional_tools": [],
    "privileged_tools": [],
    "boundary_notes": "This skill operates at the map-artifact boundary. It parses public navigation memory, Nav2 map files, and scene-engine asset partitions, records classification/actionability/review status, and must not read private cleanup scoring truth."
  },
  "capability_requirements": {
    "profiles": ["household_world"],
    "backend_variants": ["agibot_gdk", "nav2_ros2", "api_semantic_synthetic"],
    "notes": "The output is consumed by household-world tasks through runtime_map_prior, not through an Agibot-specific cleanup path."
  },
  "scripts": [
    {
      "path": "scripts/convert_navigation_memory.py",
      "kind": "converter",
      "summary": "Skill-owned wrapper around scripts/maps/convert_agibot_navigation_memory.py; deterministically parses navigation_memory.json, Nav2 YAML, occupancy grid, and source metadata into runtime_map_prior_snapshot_v1."
    },
    {
      "path": "scripts/generate_scene_room_overlay.py",
      "kind": "room_semantic_overlay",
      "summary": "Generates scene_room_semantic_overlay_v1 review evidence from scene-engine/Gaussian asset partitions. B1 runtime bundle compilation is handled by scripts/maps/compile_b1_map12_runtime_bundle.py."
    }
  ],
  "evidence_outputs": [
    "runtime_map_prior_snapshot.json",
    "materialized_targets.json",
    "room_semantic_overlay.json"
  ],
  "lifecycle": {
    "refactor_signals": [
      "multiple offline map formats need conversion into the same snapshot contract",
      "agent semantic classification overrides need review UI or batch tooling",
      "scene room labels increasingly depend on rendered visual evidence rather than partition/object-name heuristics"
    ],
    "promote_to_mcp_when": [
      "several skills need online conversion through a stable robot capability surface",
      "classification inputs and review states are stable",
      "public/private artifact boundaries remain enforceable in traces"
    ]
  }
}
