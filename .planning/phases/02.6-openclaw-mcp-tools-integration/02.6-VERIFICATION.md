---
phase: 02.6
status: passed
verified: 2026-04-21
verifier: orchestrator (live-probe evidence + pytest gate; no desk-review agent needed because this phase includes its own live-probe gate plan 02.6-06)
---

# Phase 02.6 — Verification

## Goal achievement

Phase goal (from ROADMAP): *Same user-visible outcome as Phase 2.5 (single-agent autonomous nav + human steer), rebuilt on `observe` / `move` / `done` as first-class MCP tools over streamable-http, agent running under Gateway tool `profile: minimal`.*

**Verdict: PASSED.** Every success criterion below has a concrete evidence pointer in live-probe output or a plan summary — not aspirational claims.

## Success criteria traceability

| SC | Claim | Evidence | Status |
|----|-------|----------|--------|
| 1 | `examples/openclaw_nav_autonomous.py` runs E2E, agent calls observe within 30s, produces replay.gif + report.html | Probe 4 — `output/probes/probe4/replay.gif` 826KB, `report.html` 431KB, `trace.jsonl` 440KB; Probe 2 measured observe-latency under 30s | PASS |
| 2 | Zero `exec` / `curl` / generic `image`-tool calls in Gateway log | Probe 4 narrowed JSON-`name`-field grep returns 0 | PASS |
| 3 | Human interjection appears in trace.jsonl tool-response + report.html + agent reasoning | Probe 4/5 trace.jsonl contains human_message events; report.html renders them in tool-call log | PASS |
| 4 | Per-turn prompt-token overhead ≤ 60% of coding profile *(threshold revised 2026-04-21 from ≤50% — see `docs/retrospectives/phase-2.6.md`)* | Probe 6: minimal=6,440, coding=11,335, ratio=0.568 | PASS |
| 5 | `scripts/openclaw-bootstrap.sh` seeds MCP + `profile: minimal` pre-`docker run` (no post-start `openclaw mcp set`) | Plan 02.6-02 SUMMARY + Probe 1 MCP-initialize handshake success | PASS |
| 6 | Back-to-back runs show fresh agent state (workspace reset regression guard) | Probe 5 — run 2 trace.jsonl starts at move 0, Gateway log confirms workspace dir wipe | PASS |

## Plan-level completion

All 7 plans have matching SUMMARY.md files in the phase directory:

- `02.6-01-mcp-server` → `roboclaws/openclaw/mcp_server.py` (487 lines), 14 unit tests pass
- `02.6-02-bootstrap-seeds-mcp-and-profile` → bootstrap seeds MCP + profile pre-`docker run`, 21 tests pass (1 docker-gated skip)
- `02.6-03-skill-md-shrink` → `skills/ai2thor-navigator/SKILL.md` 245→25 lines
- `02.6-04-example-rewire-to-mcp` → example rewired, 6 integration tests pass
- `02.6-05-delete-sim-server` → `sim_server.py` + test deleted, no live references remain
- `02.6-06-live-probe-gate` → 6/6 probes passed after SC#4 revision (see `02.6-LOCAL-PROBE-RESULTS.md` — 440 lines verbatim evidence)
- `02.6-07-docs-update` → retrospective + docs/openclaw-local.md + docs/openclaw-gateway-internals.md updated

## Regression gate

`env -i PATH=".venv/bin:/usr/bin:/bin" HOME=$HOME .venv/bin/pytest` — **475 passed, 1 skipped (docker-gated) in 69.58s.** No cross-phase regressions.

## Deviations from plan (all documented)

1. Plan files were renamed from `02.6-NN-slug.md` to `02.6-NN-slug-PLAN.md` pre-execution (commit `e039d57`) because the GSD SDK strictly matches `*-PLAN.md` for plan discovery. Content unchanged.
2. ROADMAP SC#4 threshold revised from ≤0.50 to ≤0.60 after Probe 6 measured 0.568 against Gateway image `:2026.4.14`. The spike's 0.408 baseline reflected an earlier Gateway config whose coding profile was 26% larger. See `docs/retrospectives/phase-2.6.md` + `TODOS.md` for optional follow-up.
3. Plan 02.6-01's Docker `host.docker.internal` assumption failed on Linux kernel 6.17 / Docker 29.2.1 — container-to-host bridge routes via `172.17.0.1`, not loopback. Fixed in wave 4 at `examples/openclaw_nav_autonomous.py` call site (`host="0.0.0.0"`), preserving mcp_server.py default. Documented in `docs/openclaw-local.md` Gotchas section.
4. Worktree isolation disabled for this phase (`.planning/config.json workflow.use_worktrees=false`) because the repo uses an editable `pip install -e .` venv that would break inside isolated worktrees. Sequential executor dispatch used instead.

## Human-needed items

None. Task 8 (final gate visual verification) was handled interactively during wave 4 — operator reviewed replay.gif, accepted SC#4 revision, filed report-UI parity + ruff-sweep items to `TODOS.md`.
