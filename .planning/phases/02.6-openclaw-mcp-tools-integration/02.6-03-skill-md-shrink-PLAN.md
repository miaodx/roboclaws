---
phase: 02.6
plan: 03
slug: skill-md-shrink
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/ai2thor-navigator/SKILL.md
autonomous: true
requirements_addressed: [A-06]

must_haves:
  truths:
    - "skills/ai2thor-navigator/SKILL.md has <=25 body lines (content after the second frontmatter '---' fence) per D-06; frontmatter is counted separately."
    - "SKILL.md contains zero references to curl, exec, /tmp, or the generic image tool."
    - "SKILL.md names the three tools exactly as the agent sees them (roboclaws__observe, roboclaws__move, roboclaws__done) plus the double-underscore convention."
    - "SKILL.md describes the observe→think→move loop and the human_message protocol in one place each."
  artifacts:
    - path: "skills/ai2thor-navigator/SKILL.md"
      provides: "Thin skill file (<=25 body lines, body-only per D-06) describing the three MCP tools + loop + human_message protocol"
      contains: "roboclaws__observe"
  key_links:
    - from: "Gateway agent system prompt"
      to: "skills/ai2thor-navigator/SKILL.md"
      via: "bind-mounted into /home/node/.openclaw/workspaces/<agent>/skills/ai2thor-navigator/SKILL.md and read on session start"
      pattern: "roboclaws__observe"
---

<objective>
Rewrite `skills/ai2thor-navigator/SKILL.md` to the thin form the Phase 2.6 architecture demands: describe the three MCP tools by their prefixed names, the loop pattern, and the human_message protocol. Drop every line that presupposes the wrong architecture — no curl recipes, no exec warnings, no `/tmp` notes, no `image` tool caveats. Under `profile: minimal` (locked in D-03) those tools literally don't exist in the agent's surface, so instructions about them waste context and risk re-teaching behaviors the spike proved destructive.

Purpose: Implements D-06 from CONTEXT.md. The SKILL.md is the single document the agent reads to understand what this workspace lets it do; shrinking it is what turns the ~59% prompt-overhead win from the spike (15,396 → 6,285 tokens) from a profile-switch artifact into a real per-turn budget gain.

Output: A rewritten SKILL.md that the agent + human reviewers can scan in under a minute, with zero Phase-2.5-era leakage.

Note on wave: this plan is wave 1 (parallel with plans 01 and 02). It only edits `skills/ai2thor-navigator/SKILL.md`, which is not touched by plan 01 (`roboclaws/openclaw/mcp_server.py`, `tests/...`, `pyproject.toml`) or plan 02 (`scripts/openclaw-bootstrap.sh`, `tests/test_openclaw_bootstrap.py`). No `files_modified` collision.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.6-openclaw-mcp-tools-integration/02.6-CONTEXT.md
@.planning/phases/02.6-openclaw-mcp-tools-integration/02.6-SPIKE-FINDINGS.md
@skills/ai2thor-navigator/SKILL.md
@skills/ai2thor-navigator/souls/

<interfaces>
Prefixed tool names the agent sees (from spike F-2; double-underscore separator):
- `roboclaws__observe` — returns FPV image + overhead image + structured state text (including optional human_message)
- `roboclaws__move` — takes `direction` (one of MoveAhead/MoveBack/MoveLeft/MoveRight/RotateLeft/RotateRight/LookUp/LookDown) and `reason` (free-form string)
- `roboclaws__done` — takes `reason` (free-form string); ends the run

SOUL files in `souls/`: aggressive.md, defensive.md, cooperative.md — unchanged by this plan.
</interfaces>

<body_line_rule>
D-06 locks the **body** of SKILL.md at ≤25 lines, where "body" = all content AFTER the second frontmatter `---` fence. Frontmatter (between the two `---` fences, inclusive) is counted separately. Verification commands below use an awk extractor that counts only non-blank body lines so blank separators between sections don't push the file over budget. Apply the same awk extractor everywhere — do NOT mix `wc -l <SKILL.md>` (which counts the entire file) with the body-only rule.
</body_line_rule>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite SKILL.md so the body (post-frontmatter) is ≤25 lines, MCP-tool-only content</name>
  <read_first>
    - skills/ai2thor-navigator/SKILL.md (full current 245-line file — identify what to keep: name, description, metadata, the SOUL reference)
    - .planning/phases/02.6-openclaw-mcp-tools-integration/02.6-CONTEXT.md (D-06 shrink target + required sections)
    - .planning/phases/02.6-openclaw-mcp-tools-integration/02.6-SPIKE-FINDINGS.md (F-2 prefix convention)
  </read_first>
  <behavior>
    - Body-only line count (content after the second `---` fence, non-blank lines only) is ≤25:
      `awk 'BEGIN{fences=0} /^---[[:space:]]*$/{fences++; next} fences>=2 && NF{print}' skills/ai2thor-navigator/SKILL.md | wc -l` returns a value ≤ 25.
    - `grep -cE "curl|exec|/tmp|image tool|data:image|base64" skills/ai2thor-navigator/SKILL.md` returns 0.
    - `grep -c "roboclaws__observe" skills/ai2thor-navigator/SKILL.md` returns >= 1 (same for `roboclaws__move` and `roboclaws__done`).
    - File starts with the existing frontmatter block (`---` → `---`) — the Gateway skill loader relies on it.
    - File contains a short bullet list of the 8 `direction` values (MoveAhead/MoveBack/MoveLeft/MoveRight/RotateLeft/RotateRight/LookUp/LookDown) so the agent knows the move argument space without making a separate tool call.
    - File contains one sentence on the observe→think→move loop and one sentence on the human_message protocol (from D-10).
  </behavior>
  <action>
    Overwrite `skills/ai2thor-navigator/SKILL.md` with this exact structure (replace the current 245-line body entirely):

    1. **Frontmatter (keep the existing block)** — copy the `---` block from the current file byte-for-byte:
       ```yaml
       ---
       name: ai2thor-navigator
       description: Navigate a simulated robot one step at a time in an AI2-THOR indoor scene. Given a first-person camera frame, an overhead map, and structured game state, return a single navigation action via MCP tools.
       metadata:
         openclaw:
           emoji: 🤖
       ---
       ```
       (Tweak the `description` field's last clause from "return a single navigation action as JSON" to "…via MCP tools" — aligns with the new contract.)

    2. **Role (1 paragraph, ≤3 sentences):**
       ```markdown
       # AI2-THOR Navigator

       You drive one simulated agent through an AI2-THOR indoor room using the MCP tools exposed by the `roboclaws` server. Each run is one agent, one kickoff call, and a long wall-clock budget; pull what you need with the three tools below until the budget is exhausted or you call done.
       ```

    3. **Tools (one line each, using prefixed names):**
       ```markdown
       ## Tools

       - `roboclaws__observe()` — returns the first-person camera frame, the overhead map, and the current structured state (position, yaw, scene, step count, budget_remaining, optional `human_message`). No arguments.
       - `roboclaws__move(direction, reason)` — take one physical step. `direction` must be one of: `MoveAhead`, `MoveBack`, `MoveLeft`, `MoveRight`, `RotateLeft`, `RotateRight`, `LookUp`, `LookDown`. `reason` is a short natural-language string used for replay narration.
       - `roboclaws__done(reason)` — end the run cleanly. Call when the goal is achieved or you are stuck.
       ```

    4. **Loop (one paragraph):**
       ```markdown
       ## Loop

       Default pattern: `observe → think → move`. The `observe` tool is how you see the world — call it first, and again whenever you need fresh frames. `move` returns structured state only (no images), so re-observe after each meaningful step. You may take a short burst of moves without re-observing if you have a concrete local reason (clear hallway, safe backtrack, following a human directive); include that justification in `move.reason`.
       ```

    5. **Human messages (one paragraph, from D-10):**
       ```markdown
       ## Human messages

       If an observe or move response includes a non-null `human_message` in the state, treat it as a directive from an observer. Acknowledge it in your next `move.reason`, act on it when compatible with the current goal, and do not call `done` until you have taken at least one follow-up action. When you eventually do call `done`, mention the human_message and how you addressed it in the done reason.
       ```

    After writing, run the body-line extractor to confirm the body fits:
    ```bash
    body_lines=$(awk 'BEGIN{fences=0} /^---[[:space:]]*$/{fences++; next} fences>=2 && NF{print}' skills/ai2thor-navigator/SKILL.md | wc -l)
    echo "body_lines=$body_lines"
    test "$body_lines" -le 25
    ```
    If `body_lines` exceeds 25, drop the optional SOUL pointer (see next paragraph) first; if still over, tighten the Loop paragraph to two sentences.

    Optional but helpful (only if the body-line extractor still reports ≤25 with it included): a one-line pointer to the SOULs directory (`See souls/aggressive.md, souls/defensive.md, souls/cooperative.md for play-style presets.`). If including it pushes the body over 25 non-blank lines, drop it — the SOULs are loaded by the bootstrap script into `SOUL.md`, not referenced by the agent through this skill file.

    DO NOT include: Python usage examples, cost tables, architecture diagrams, multi-agent Phase-2 notes, curl recipes, HTTP endpoint signatures, `/tmp` warnings, `image` tool notes, `data:image` payloads, file-path caveats. Every one of those was inherited from the Phase 2.5 contract and is actively misleading under the MCP/minimal-profile contract.

    DO NOT rename the skill or change the frontmatter `name:` field — the bootstrap script relies on `skills/ai2thor-navigator/` as the directory.
  </action>
  <verify>
    <automated>bash -c 'f=/home/mi/ws/gogo/roboclaws/skills/ai2thor-navigator/SKILL.md; body_lines=$(awk "BEGIN{fences=0} /^---[[:space:]]*\$/{fences++; next} fences>=2 && NF{print}" "$f" | wc -l); forbidden=$(grep -cE "curl|exec|/tmp|image tool|data:image|base64" "$f"); tools=$(grep -c "roboclaws__observe\|roboclaws__move\|roboclaws__done" "$f"); echo "body_lines=$body_lines forbidden=$forbidden tools=$tools"; [[ $body_lines -le 25 && $forbidden -eq 0 && $tools -ge 3 ]]'</automated>
  </verify>
  <done>SKILL.md body (content after the second `---` fence, non-blank) is ≤25 lines, contains zero forbidden substrings (curl/exec/`/tmp`/image tool/data:image/base64), and all three prefixed tool names appear at least once. `grep -c "^---$" SKILL.md` returns 2 (frontmatter preserved).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SKILL.md ↔ agent's operating instructions | The agent reads this file verbatim on session start. Anything in it becomes part of the system prompt that drives the run. Misleading instructions re-create the Phase 2.5 failure mode. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.6-10 | Repudiation | Stale Phase-2.5 curl/exec instructions survive the rewrite | mitigate | Verify step greps for `curl|exec|/tmp|image tool|data:image|base64` and requires 0 matches. This is the whole point of the shrink. |
| T-02.6-11 | Information Disclosure | SKILL.md would leak internal endpoint URLs if it kept the old Phase 2.5 content | mitigate (by removal) | All `http://host.docker.internal:18788` references are deleted. The new MCP URL is in openclaw.json (container-internal), not SKILL.md (which lives in the workspace and is agent-visible). |
| T-02.6-12 | Tampering | A developer adds back a "quick note about the image tool" in a future edit | accept | Out of scope for this plan. If it becomes a recurring issue, add a CI lint that greps SKILL.md for the forbidden substrings. Noted here but not built. |

No `high`-severity threats; plan proceeds.
</threat_model>

<verification>
- Body-only non-blank line count:
  ```bash
  awk 'BEGIN{fences=0} /^---[[:space:]]*$/{fences++; next} fences>=2 && NF{print}' skills/ai2thor-navigator/SKILL.md | wc -l
  ```
  prints a number ≤ 25.
- `grep -cE "curl|exec|/tmp|image tool|data:image|base64" skills/ai2thor-navigator/SKILL.md` prints `0`.
- `grep -c "roboclaws__observe\|roboclaws__move\|roboclaws__done" skills/ai2thor-navigator/SKILL.md` prints >= 3.
- `head -n 1 skills/ai2thor-navigator/SKILL.md` prints `---` (frontmatter preserved).
- `grep "^name: ai2thor-navigator$" skills/ai2thor-navigator/SKILL.md` matches.
</verification>

<success_criteria>
- SKILL.md body (content after the second `---` fence, non-blank lines only) is ≤25 lines (D-06 — body-only budget, frontmatter counted separately).
- Zero forbidden substrings (curl, exec, /tmp, image tool, data:image, base64).
- Three prefixed tool names present.
- Loop pattern + human_message protocol each documented in one short paragraph.
- Frontmatter shape unchanged; skill directory name unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/02.6-openclaw-mcp-tools-integration/02.6-03-SUMMARY.md`.
</output>
