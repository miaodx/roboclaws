---
plan: "05-05"
title: "Simplify roboclaws/games/coverage.py and roboclaws/games/territory.py"
wave: 1
depends_on: []
files_modified:
  - roboclaws/games/coverage.py
  - roboclaws/games/territory.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "coverage.py and territory.py pass /simplify review with no high-severity findings"
    - "pytest exits 0 after changes"
    - "ruff check and ruff format --check exit 0"
    - "net line count is <= 640 for coverage.py (original)"
    - "net line count is <= 413 for territory.py (original)"
    - "no public function or class signatures changed"
  artifacts:
    - path: "roboclaws/games/coverage.py"
      provides: "Simplified cooperative coverage game logic"
    - path: "roboclaws/games/territory.py"
      provides: "Simplified territory control game logic"
  key_links:
    - from: "roboclaws/games/coverage.py"
      to: "tests/test_coverage.py, tests/test_coverage_example.py"
      via: "import CoverageGame"
      pattern: "from roboclaws.games.coverage import\|from roboclaws.games import.*coverage"
    - from: "roboclaws/games/territory.py"
      to: "tests/test_territory.py, tests/test_territory_example.py"
      via: "import TerritoryGame"
      pattern: "from roboclaws.games.territory import\|from roboclaws.games import.*territory"
---

<objective>
Simplify roboclaws/games/coverage.py (640 lines) and roboclaws/games/territory.py (413 lines) — remove dead code, consolidate shared game-loop helpers, improve readability.
Tests green. No public API changes.

Purpose: These two game files share similar turn-loop, scoring, and prompt-assembly patterns. A /simplify pass on both together is efficient: simplify each independently, but note cross-file opportunities where the same pattern can be expressed consistently.
Output: Both files committed atomically (one commit each) with tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task id="T1" type="auto">
  <title>Simplify roboclaws/games/coverage.py</title>
  <read_first>
    - roboclaws/games/coverage.py
    - tests/test_coverage.py
    - tests/test_coverage_example.py
    - CLAUDE.md
  </read_first>
  <action>
    1. Run /simplify on roboclaws/games/coverage.py
    2. Review findings and apply:
       - Remove dead helper functions not called outside the module
       - Consolidate duplicated turn-loop or grid-update logic
       - Simplify verbose scoring or prompt-assembly chains
       - Improve names where ambiguous (without changing public API)
       - Remove unused imports
    3. Grep callers to verify no public signatures changed:
       grep -rn "from roboclaws.games.coverage import\|from roboclaws.games import" roboclaws/ examples/ tests/ scripts/ --include="*.py"
    4. Run: pytest tests/test_coverage.py tests/test_coverage_example.py -x -q
    5. Run: pytest -x -q (full suite)
    6. Run: ruff check . && ruff format --check .
    7. Commit: git commit -m "refactor(coverage): simplify pass"
  </action>
  <acceptance_criteria>
    - pytest exits 0 (full suite)
    - ruff check exits 0
    - ruff format --check exits 0
    - git log --oneline -1 contains "refactor(coverage): simplify pass"
    - No public signatures changed (grep confirms)
    - net line count for coverage.py is <= 640
  </acceptance_criteria>
</task>

<task id="T2" type="auto">
  <title>Simplify roboclaws/games/territory.py</title>
  <read_first>
    - roboclaws/games/territory.py
    - tests/test_territory.py
    - tests/test_territory_example.py
    - CLAUDE.md
  </read_first>
  <action>
    1. Run /simplify on roboclaws/games/territory.py
    2. Review findings and apply:
       - Remove dead helper functions not called outside the module
       - Consolidate duplicated turn-loop or scoring logic
       - Where coverage.py and territory.py share an identical private pattern
         (e.g. same scoring helper), express it consistently — but do NOT extract
         to a shared module (that would add new abstraction, not remove complexity)
       - Improve names where ambiguous (without changing public API)
       - Remove unused imports
    3. Grep callers to verify no public signatures changed:
       grep -rn "from roboclaws.games.territory import\|from roboclaws.games import" roboclaws/ examples/ tests/ scripts/ --include="*.py"
    4. Run: pytest tests/test_territory.py tests/test_territory_example.py -x -q
    5. Run: pytest -x -q (full suite)
    6. Run: ruff check . && ruff format --check .
    7. Commit: git commit -m "refactor(territory): simplify pass"
  </action>
  <acceptance_criteria>
    - pytest exits 0 (full suite)
    - ruff check exits 0
    - ruff format --check exits 0
    - git log --oneline -1 contains "refactor(territory): simplify pass"
    - No public signatures changed (grep confirms)
    - net line count for territory.py is <= 413
  </acceptance_criteria>
</task>

</tasks>

## Must Haves
- Tests green after this plan (pytest exits 0 after each commit)
- Ruff clean (check + format)
- No public API regressions
- Net change is a reduction or neutral (no new complexity added)
- Do NOT extract a shared game-utilities module — simplify within each file only

<verification>
pytest tests/test_coverage.py tests/test_coverage_example.py tests/test_territory.py tests/test_territory_example.py -x -q
pytest -x -q
ruff check .
ruff format --check .
</verification>

<success_criteria>
coverage.py and territory.py simplified and committed atomically (two commits). pytest green throughout. Ruff clean. Line counts <= originals.
</success_criteria>

<output>
After completion, create `.planning/phases/05-iterative-codebase-simplification/05-05-SUMMARY.md`
</output>
