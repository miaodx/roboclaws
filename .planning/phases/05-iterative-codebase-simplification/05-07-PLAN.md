---
plan: "05-07"
title: "Simplify bridge.py, vision_bridge.py, views.py, and replay.py"
wave: 2
depends_on: ["05-04", "05-01"]
files_modified:
  - roboclaws/openclaw/bridge.py
  - roboclaws/openclaw/vision_bridge.py
  - roboclaws/core/views.py
  - roboclaws/core/replay.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "bridge.py, vision_bridge.py, views.py, and replay.py pass /simplify review with no high-severity findings"
    - "pytest exits 0 after changes"
    - "ruff check and ruff format --check exit 0"
    - "net line counts at or below originals: bridge.py<=302, vision_bridge.py<=278, views.py<=268, replay.py<=429"
    - "no public function or class signatures changed"
  artifacts:
    - path: "roboclaws/openclaw/bridge.py"
      provides: "Simplified OpenClaw bridge (post Phase 2.6 split)"
    - path: "roboclaws/openclaw/vision_bridge.py"
      provides: "Simplified vision bridge"
    - path: "roboclaws/core/views.py"
      provides: "Simplified shared view primitives"
    - path: "roboclaws/core/replay.py"
      provides: "Simplified replay recorder"
  key_links:
    - from: "roboclaws/openclaw/bridge.py"
      to: "tests/test_bridge.py, tests/test_bridge_start_run.py"
      via: "import OpenClawBridge"
      pattern: "from roboclaws.openclaw.bridge import\|from roboclaws.openclaw import.*bridge"
    - from: "roboclaws/openclaw/vision_bridge.py"
      to: "tests/test_openclaw_vision_bridge.py"
      via: "import VisionBridge"
      pattern: "from roboclaws.openclaw.vision_bridge import\|from roboclaws.openclaw import.*vision_bridge"
    - from: "roboclaws/core/views.py"
      to: "tests/test_views.py"
      via: "import view builders"
      pattern: "from roboclaws.core.views import\|from roboclaws.core import.*views"
    - from: "roboclaws/core/replay.py"
      to: "tests/test_replay.py"
      via: "import ReplayRecorder"
      pattern: "from roboclaws.core.replay import\|from roboclaws.core import.*replay"
---

<objective>
Simplify roboclaws/openclaw/bridge.py (302 lines), roboclaws/openclaw/vision_bridge.py (278 lines), roboclaws/core/views.py (268 lines), and roboclaws/core/replay.py (429 lines) — remove dead code, consolidate helpers, improve readability.
Tests green. No public API changes.

Purpose: These four supporting files are mid-size (268–429 lines) and were last touched during the Phase 2.6 transport split. A /simplify pass ensures no dead code was left behind from the refactor and that each file's internal structure is as clean as possible.
Output: Four files committed atomically (one commit per file group) with tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task id="T1" type="auto">
  <title>Simplify roboclaws/openclaw/bridge.py and roboclaws/openclaw/vision_bridge.py</title>
  <read_first>
    - roboclaws/openclaw/bridge.py
    - roboclaws/openclaw/vision_bridge.py
    - tests/test_bridge.py
    - tests/test_bridge_start_run.py
    - tests/test_openclaw_vision_bridge.py
    - CLAUDE.md
  </read_first>
  <action>
    1. Run /simplify on roboclaws/openclaw/bridge.py
    2. Run /simplify on roboclaws/openclaw/vision_bridge.py
    3. Review findings for both and apply:
       - Remove dead methods left behind from the transport split in 260423-q71
       - Consolidate duplicated request-preparation or response-parsing patterns
       - Improve names where ambiguous (without changing public API)
       - Remove unused imports
    4. Grep callers to verify no public signatures changed:
       grep -rn "from roboclaws.openclaw.bridge import\|from roboclaws.openclaw.vision_bridge import" roboclaws/ examples/ tests/ scripts/ --include="*.py"
    5. Run: pytest tests/test_bridge.py tests/test_bridge_start_run.py tests/test_openclaw_vision_bridge.py -x -q
    6. Run: pytest -x -q (full suite)
    7. Run: ruff check . && ruff format --check .
    8. Commit: git commit -m "refactor(bridge): simplify pass"
  </action>
  <acceptance_criteria>
    - pytest exits 0 (full suite)
    - ruff check exits 0
    - ruff format --check exits 0
    - git log --oneline -1 contains "refactor(bridge): simplify pass"
    - No public signatures changed (grep confirms)
    - net line count for bridge.py is <= 302 and vision_bridge.py is <= 278
  </acceptance_criteria>
</task>

<task id="T2" type="auto">
  <title>Simplify roboclaws/core/views.py and roboclaws/core/replay.py</title>
  <read_first>
    - roboclaws/core/views.py
    - roboclaws/core/replay.py
    - tests/test_views.py
    - tests/test_replay.py
    - CLAUDE.md
  </read_first>
  <action>
    1. Run /simplify on roboclaws/core/views.py
    2. Run /simplify on roboclaws/core/replay.py
    3. Review findings for both and apply:
       - Remove dead helper functions or unused view-builder paths
       - Consolidate duplicated frame-recording or state-serialization patterns in replay.py
       - Simplify verbose view-composition chains in views.py
       - Improve names where ambiguous (without changing public API)
       - Remove unused imports
    4. Grep callers to verify no public signatures changed:
       grep -rn "from roboclaws.core.views import\|from roboclaws.core.replay import" roboclaws/ examples/ tests/ scripts/ --include="*.py"
    5. Run: pytest tests/test_views.py tests/test_replay.py -x -q
    6. Run: pytest -x -q (full suite)
    7. Run: ruff check . && ruff format --check .
    8. Commit: git commit -m "refactor(views,replay): simplify pass"
  </action>
  <acceptance_criteria>
    - pytest exits 0 (full suite)
    - ruff check exits 0
    - ruff format --check exits 0
    - git log --oneline -1 contains "refactor(views,replay): simplify pass"
    - No public signatures changed (grep confirms)
    - net line count for views.py is <= 268 and replay.py is <= 429
  </acceptance_criteria>
</task>

</tasks>

## Must Haves
- Tests green after this plan (pytest exits 0 after each commit)
- Ruff clean (check + format)
- No public API regressions
- Net change is a reduction or neutral (no new complexity added)

<verification>
pytest tests/test_bridge.py tests/test_bridge_start_run.py tests/test_openclaw_vision_bridge.py tests/test_views.py tests/test_replay.py -x -q
pytest -x -q
ruff check .
ruff format --check .
</verification>

<success_criteria>
bridge.py, vision_bridge.py, views.py, and replay.py simplified and committed atomically (two commits). pytest green throughout. Ruff clean. Line counts <= originals.
</success_criteria>

<output>
After completion, create `.planning/phases/05-iterative-codebase-simplification/05-07-SUMMARY.md`
</output>
