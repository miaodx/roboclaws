---
plan: "05-02"
title: "Simplify roboclaws/openclaw/mcp_server.py"
wave: 3
depends_on: ["05-07"]
files_modified:
  - roboclaws/openclaw/mcp_server.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "mcp_server.py passes /simplify review with no high-severity findings"
    - "pytest exits 0 after changes"
    - "ruff check and ruff format --check exit 0"
    - "net line count is <= 927 (original)"
    - "no public function or class signatures changed"
  artifacts:
    - path: "roboclaws/openclaw/mcp_server.py"
      provides: "Simplified FastMCP server with observe/move/done tools"
  key_links:
    - from: "roboclaws/openclaw/mcp_server.py"
      to: "tests/test_openclaw_mcp_server.py"
      via: "import mcp_server"
      pattern: "from roboclaws.openclaw.mcp_server import\|from roboclaws.openclaw import.*mcp_server"
---

<objective>
Simplify roboclaws/openclaw/mcp_server.py (927 lines) — remove dead code, consolidate helpers, improve readability.
Tests green. No public API changes.

Purpose: mcp_server.py is the second-largest file and the MCP tool surface shipped in Phase 2.6. A /simplify pass will surface internal helpers that can be inlined, verbose error handling patterns that can be consolidated, and any dead code paths left from the sim_server migration.
Output: Leaner mcp_server.py committed atomically with tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task id="T1" type="auto">
  <title>Simplify roboclaws/openclaw/mcp_server.py</title>
  <read_first>
    - roboclaws/openclaw/mcp_server.py
    - tests/test_openclaw_mcp_server.py
    - CLAUDE.md
  </read_first>
  <action>
    1. Run /simplify on roboclaws/openclaw/mcp_server.py
    2. Review findings and apply:
       - Remove any dead helper functions not reachable from the MCP tool surface
       - Consolidate repeated error-response patterns
       - Simplify overly verbose state-management logic
       - Improve names where ambiguous (without changing public MCP tool names or HTTP endpoints)
       - Remove unused imports
    3. SKILL.md contract guard — verify all MCP response field names documented in
       skills/ai2thor-navigator/SKILL.md still exist in mcp_server.py after changes:
       grep -n "pose_delta\|visited_count_here\|collisions_total\|moves_since_observe\|observe_delivery\|view_variant\|image_labels\|bridge_model\|warning" roboclaws/openclaw/mcp_server.py
       All 9 field names must appear. If any are missing, either restore the field or
       update SKILL.md to match before committing.
    4. Grep callers to verify no public signatures changed:
       grep -rn "from roboclaws.openclaw.mcp_server import\|from roboclaws.openclaw import" roboclaws/ examples/ tests/ scripts/ --include="*.py"
    5. Run: pytest tests/test_openclaw_mcp_server.py -x -q
    6. Run: pytest -x -q (full suite)
    7. Run: ruff check . && ruff format --check .
    8. Commit: git commit -m "refactor(mcp_server): simplify pass"
  </action>
  <acceptance_criteria>
    - pytest exits 0 (full suite)
    - ruff check exits 0
    - ruff format --check exits 0
    - git log --oneline -1 contains "refactor(mcp_server): simplify pass"
    - No public MCP tool names, HTTP endpoints, or Python API signatures changed (grep on callers confirms)
    - net line count is <= 927
  </acceptance_criteria>
</task>

</tasks>

## Must Haves
- Tests green after this plan (pytest exits 0)
- Ruff clean (check + format)
- No public API regressions
- Net change is a reduction or neutral (no new complexity added)

<verification>
pytest tests/test_openclaw_mcp_server.py -x -q
pytest -x -q
ruff check .
ruff format --check .
</verification>

<success_criteria>
mcp_server.py simplified and committed atomically. pytest green. Ruff clean. Line count <= 927.
</success_criteria>

<output>
After completion, create `.planning/phases/05-iterative-codebase-simplification/05-02-SUMMARY.md`
</output>
