---
plan: "05-08"
title: "Simplify examples/openclaw_demo.py, openclaw_interactive.py, and openclaw_nav_autonomous.py"
wave: 4
depends_on: ["05-07", "05-02"]
files_modified:
  - examples/openclaw_demo.py
  - examples/openclaw_interactive.py
  - examples/openclaw_nav_autonomous.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "openclaw_demo.py, openclaw_interactive.py, and openclaw_nav_autonomous.py pass /simplify review with no high-severity findings"
    - "pytest exits 0 after changes"
    - "ruff check and ruff format --check exit 0"
    - "net line counts at or below originals: openclaw_demo.py<=416, openclaw_interactive.py<=395, openclaw_nav_autonomous.py<=467"
    - "no CLI argument signatures changed"
  artifacts:
    - path: "examples/openclaw_demo.py"
      provides: "Simplified single-agent OpenClaw demo"
    - path: "examples/openclaw_interactive.py"
      provides: "Simplified interactive OpenClaw example"
    - path: "examples/openclaw_nav_autonomous.py"
      provides: "Simplified autonomous navigation example"
  key_links:
    - from: "examples/openclaw_demo.py"
      to: "tests/test_openclaw_demo.py"
      via: "import demo module"
      pattern: "from examples.openclaw_demo import\|import openclaw_demo"
    - from: "examples/openclaw_interactive.py"
      to: "tests/test_openclaw_interactive.py"
      via: "import interactive module"
      pattern: "from examples.openclaw_interactive import\|import openclaw_interactive"
    - from: "examples/openclaw_nav_autonomous.py"
      to: "tests/test_openclaw_nav_autonomous.py"
      via: "import autonomous module"
      pattern: "from examples.openclaw_nav_autonomous import\|import openclaw_nav_autonomous"
---

<objective>
Simplify examples/openclaw_demo.py (416 lines), examples/openclaw_interactive.py (395 lines), and examples/openclaw_nav_autonomous.py (467 lines) — remove dead code, consolidate shared utility patterns, improve readability.
Tests green. No CLI argument changes.

Purpose: These three OpenClaw example scripts share similar CLI boilerplate, bootstrap-call patterns, and result-saving logic. A /simplify pass will surface duplicated arg-parsing setup, redundant error-handling chains, and any dead code paths left from previous iterations.
Output: Three files committed atomically (one commit for all three) with tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task id="T1" type="auto">
  <title>Simplify examples/openclaw_demo.py, openclaw_interactive.py, and openclaw_nav_autonomous.py</title>
  <read_first>
    - examples/openclaw_demo.py
    - examples/openclaw_interactive.py
    - examples/openclaw_nav_autonomous.py
    - tests/test_openclaw_demo.py
    - tests/test_openclaw_interactive.py
    - tests/test_openclaw_nav_autonomous.py
    - CLAUDE.md
  </read_first>
  <action>
    1. Run /simplify on examples/openclaw_demo.py
    2. Run /simplify on examples/openclaw_interactive.py
    3. Run /simplify on examples/openclaw_nav_autonomous.py
    4. Review findings for all three and apply:
       - Remove dead code paths (unused flags, unreachable branches)
       - Consolidate shared CLI-boilerplate patterns within each file
         (do NOT extract a shared examples/ utility module — simplify within each file)
       - Simplify verbose result-saving or error-handling chains
       - Improve names where ambiguous
       - Remove unused imports
    5. Verify no CLI argument signatures changed (--help output must stay the same):
       python examples/openclaw_demo.py --help
       python examples/openclaw_interactive.py --help
       python examples/openclaw_nav_autonomous.py --help
    6. Run: pytest tests/test_openclaw_demo.py tests/test_openclaw_interactive.py tests/test_openclaw_nav_autonomous.py -x -q
    7. Run: pytest -x -q (full suite)
    8. Run: ruff check . && ruff format --check .
    9. Commit: git commit -m "refactor(openclaw-examples): simplify pass"
  </action>
  <acceptance_criteria>
    - pytest exits 0 (full suite)
    - ruff check exits 0
    - ruff format --check exits 0
    - git log --oneline -1 contains "refactor(openclaw-examples): simplify pass"
    - --help output unchanged for all three scripts
    - net line counts: openclaw_demo.py<=416, openclaw_interactive.py<=395, openclaw_nav_autonomous.py<=467
  </acceptance_criteria>
</task>

</tasks>

## Must Haves
- Tests green after this plan (pytest exits 0)
- Ruff clean (check + format)
- No CLI argument regressions (--help unchanged)
- Net change is a reduction or neutral (no new complexity added)
- Do NOT extract a shared examples/ utility module

<verification>
pytest tests/test_openclaw_demo.py tests/test_openclaw_interactive.py tests/test_openclaw_nav_autonomous.py -x -q
pytest -x -q
ruff check .
ruff format --check .
</verification>

<success_criteria>
openclaw_demo.py, openclaw_interactive.py, and openclaw_nav_autonomous.py simplified and committed atomically (one commit). pytest green. Ruff clean. Line counts <= originals.
</success_criteria>

<output>
After completion, create `.planning/phases/05-iterative-codebase-simplification/05-08-SUMMARY.md`
</output>
