---
plan: "05-06"
title: "Simplify roboclaws/core/vlm.py, roboclaws/core/providers/kimi.py, and roboclaws/core/providers/openai.py"
wave: 1
depends_on: []
files_modified:
  - roboclaws/core/vlm.py
  - roboclaws/core/providers/kimi.py
  - roboclaws/core/providers/openai.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "vlm.py, kimi.py, and openai.py pass /simplify review with no high-severity findings"
    - "pytest exits 0 after changes"
    - "ruff check and ruff format --check exit 0"
    - "net line count for vlm.py is <= 389 (original)"
    - "net line count for kimi.py is <= 452 (original)"
    - "net line count for openai.py is <= 222 (original)"
    - "no public function or class signatures changed"
  artifacts:
    - path: "roboclaws/core/vlm.py"
      provides: "Simplified unified VLM API interface"
    - path: "roboclaws/core/providers/kimi.py"
      provides: "Simplified Kimi provider"
    - path: "roboclaws/core/providers/openai.py"
      provides: "Simplified OpenAI provider"
  key_links:
    - from: "roboclaws/core/vlm.py"
      to: "tests/test_vlm.py"
      via: "import VLMProvider"
      pattern: "from roboclaws.core.vlm import\|from roboclaws.core import.*vlm"
    - from: "roboclaws/core/providers/kimi.py"
      to: "tests/test_vlm.py"
      via: "import KimiProvider"
      pattern: "from roboclaws.core.providers.kimi import\|from roboclaws.core.providers import.*kimi"
---

<objective>
Simplify roboclaws/core/vlm.py (389 lines), roboclaws/core/providers/kimi.py (452 lines), and roboclaws/core/providers/openai.py (222 lines) — remove dead code, consolidate provider patterns, improve readability.
Tests green. No public API changes.

Purpose: The VLM layer was split into provider files as part of 260423-q71. A /simplify pass on the full provider group ensures the split is clean: no duplicated request-building patterns, no dead retry scaffolding, and consistent error-handling across providers.
Output: Three files committed atomically (one commit per file) with tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task id="T1" type="auto">
  <title>Simplify roboclaws/core/vlm.py</title>
  <read_first>
    - roboclaws/core/vlm.py
    - tests/test_vlm.py
    - CLAUDE.md
  </read_first>
  <action>
    1. Run /simplify on roboclaws/core/vlm.py
    2. Review findings and apply:
       - Remove dead dispatch or fallback logic not reachable from public API
       - Consolidate repeated provider-routing patterns
       - Improve names where ambiguous (without changing public API)
       - Remove unused imports
    3. Grep callers to verify no public signatures changed:
       grep -rn "from roboclaws.core.vlm import\|from roboclaws.core import.*vlm" roboclaws/ examples/ tests/ scripts/ --include="*.py"
    4. Run: pytest tests/test_vlm.py tests/test_provider_retry.py -x -q
    5. Run: pytest -x -q (full suite)
    6. Run: ruff check . && ruff format --check .
    7. Commit: git commit -m "refactor(vlm): simplify pass"
  </action>
  <acceptance_criteria>
    - pytest exits 0 (full suite)
    - ruff check exits 0
    - ruff format --check exits 0
    - git log --oneline -1 contains "refactor(vlm): simplify pass"
    - No public signatures changed (grep confirms)
    - net line count for vlm.py is <= 389
  </acceptance_criteria>
</task>

<task id="T2" type="auto">
  <title>Simplify roboclaws/core/providers/kimi.py and roboclaws/core/providers/openai.py</title>
  <read_first>
    - roboclaws/core/providers/kimi.py
    - roboclaws/core/providers/openai.py
    - tests/test_vlm.py
    - tests/test_provider_retry.py
    - tests/test_nvidia_provider.py
    - CLAUDE.md
  </read_first>
  <action>
    1. Run /simplify on roboclaws/core/providers/kimi.py
    2. Run /simplify on roboclaws/core/providers/openai.py
    3. Review findings for both and apply:
       - Remove dead helper methods not called from the provider's public interface
       - Consolidate request-building patterns that are duplicated between the two providers
         (consolidate within each file — do NOT merge provider files)
       - Simplify verbose retry or timeout logic
       - Improve names where ambiguous
       - Remove unused imports
    4. Grep callers to verify no public signatures changed for either provider:
       grep -rn "from roboclaws.core.providers import\|from roboclaws.core.providers.kimi import\|from roboclaws.core.providers.openai import" roboclaws/ examples/ tests/ scripts/ --include="*.py"
    5. Run: pytest tests/test_vlm.py tests/test_provider_retry.py tests/test_nvidia_provider.py -x -q
    6. Run: pytest -x -q (full suite)
    7. Run: ruff check . && ruff format --check .
    8. Commit: git commit -m "refactor(providers): simplify kimi and openai pass"
  </action>
  <acceptance_criteria>
    - pytest exits 0 (full suite)
    - ruff check exits 0
    - ruff format --check exits 0
    - git log --oneline -1 contains "refactor(providers): simplify kimi and openai pass"
    - No public signatures changed for either provider (grep confirms)
    - net line count for kimi.py is <= 452 and openai.py is <= 222
  </acceptance_criteria>
</task>

</tasks>

## Must Haves
- Tests green after this plan (pytest exits 0 after each commit)
- Ruff clean (check + format)
- No public API regressions
- Net change is a reduction or neutral (no new complexity added)
- Do NOT merge provider files — each provider stays in its own file

<verification>
pytest tests/test_vlm.py tests/test_provider_retry.py tests/test_nvidia_provider.py -x -q
pytest -x -q
ruff check .
ruff format --check .
</verification>

<success_criteria>
vlm.py, kimi.py, and openai.py simplified and committed atomically (two commits). pytest green throughout. Ruff clean. Line counts <= originals.
</success_criteria>

<output>
After completion, create `.planning/phases/05-iterative-codebase-simplification/05-06-SUMMARY.md`
</output>
