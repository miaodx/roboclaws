---
phase: 02.7
plan: 04
slug: local-dev-validation-and-writeup
type: execute
wave: 4
depends_on: ["02.7-03"]
files_modified:
  - roboclaws/openclaw/bridge.py
  - .planning/phases/02.7-openclaw-intermediate-message-capture/02.7-SPIKE-FINDINGS.md
  - .planning/phases/02.7-openclaw-intermediate-message-capture/02.7-LOCAL-PROBE-RESULTS.md
  - docs/openclaw-local.md
autonomous: false
requirements_addressed: [A-06]

must_haves:
  truths:
    - "A real autonomous run produces transcript-bearing artifacts under the shipped default mode."
    - "A compare run records the non-default mode as well, so the repo documents what the loser looks like in practice."
    - "If real autonomous validation overturns the Plan-01 spike verdict, the repo does not stop at a write-up: the bridge default and the earlier spike record are updated before the phase is called done."
    - "`02.7-LOCAL-PROBE-RESULTS.md` records artifact paths, command lines, and the final shipped verdict using dated local evidence."
    - "`docs/openclaw-local.md` tells operators where transcript data appears (`trace.jsonl`, `run_result.json`, `report.html`) and what `terminal-body` means if that is the fallback."
    - "No bearer token or API key is copied into repo-tracked probe notes."
  artifacts:
    - path: ".planning/phases/02.7-openclaw-intermediate-message-capture/02.7-LOCAL-PROBE-RESULTS.md"
      provides: "Local-dev evidence for the shipped transcript path and its comparison run"
      contains: "Transcript source"
    - path: "docs/openclaw-local.md"
      provides: "Operator-facing transcript expectations for the autonomous MCP flow"
      contains: "Transcript capture"
  key_links:
    - from: "docs/openclaw-local.md"
      to: ".planning/phases/02.7-openclaw-intermediate-message-capture/02.7-LOCAL-PROBE-RESULTS.md"
      via: "link from transcript section to the local evidence write-up"
      pattern: "02.7-LOCAL-PROBE-RESULTS"
---

<objective>
Validate the shipped transcript path on a real local workstation, record what actually happened, and document it for future operators. This is where the phase proves whether the Plan-01 verdict survives a real autonomous run with tools, frames, and a real provider-backed Gateway.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

THIS PLAN IS LOCAL-DEV ONLY. It requires Docker, AI2-THOR, and a real provider
key. A cloud session must not pretend to execute it.
</execution_context>

<context>
@.planning/phases/02.7-openclaw-intermediate-message-capture/02.7-SPIKE-FINDINGS.md
@.planning/phases/02.7-openclaw-intermediate-message-capture/02.7-RESEARCH.md
@docs/openclaw-local.md
@examples/openclaw_nav_autonomous.py
@scripts/render_autonomous_replay.py
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Pre-flight — confirm local autonomous transcript validation is actually possible</name>
  <read_first>
    - AGENTS.md § 1 and § 7
    - docs/openclaw-local.md
  </read_first>
  <behavior>
    - The operator confirms Docker, AI2-THOR, and a real provider key are available.
    - No stale `openclaw-gateway` container is occupying the expected ports.
    - This plan does not proceed in a cloud-only session.
  </behavior>
  <action>
    Run and paste the outputs of:

    1. `docker --version`
    2. `python -c "import ai2thor; print(ai2thor.__version__)"`
    3. `[[ -n \"$KIMI_API_KEY\" || -n \"$NV_API_KEY\" ]] && echo provider-set || echo provider-missing`
    4. `docker ps -a --format '{{.Names}}' | grep -x openclaw-gateway || echo absent`
    5. `env -i PATH=\".venv/bin:/usr/bin:/bin\" HOME=$HOME .venv/bin/pytest tests/test_bridge.py tests/test_bridge_start_run.py tests/test_openclaw_nav_autonomous.py tests/test_render_autonomous_replay.py -q`

    If this is a cloud session, stop here and defer.
  </action>
  <verify>
    <automated>MISSING — checkpoint task; operator confirms local readiness</automated>
  </verify>
  <done>Operator confirms a real local transcript-validation session is available.</done>
</task>

<task type="auto">
  <name>Task 2: Run the shipped mode and the compare mode; write `02.7-LOCAL-PROBE-RESULTS.md`</name>
  <read_first>
    - .planning/phases/02.7-openclaw-intermediate-message-capture/02.7-SPIKE-FINDINGS.md
    - examples/openclaw_nav_autonomous.py
    - docs/openclaw-local.md
  </read_first>
  <behavior>
    - One run uses the shipped default mode from Plan 01's verdict.
    - A second run uses the non-default compare mode.
    - At least the shipped mode produces transcript-bearing artifacts:
      - `trace.jsonl` contains transcript data
      - `run_result.json` contains transcript metadata
      - `report.html` shows transcript UI
    - The local write-up explicitly checks transcript timing alignment against nearby tool events; it does not stop at "transcript rows exist".
    - The local write-up records whether the Plan-01 verdict held up in a real autonomous run.
    - If the compare run clearly beats the shipped default, this task updates the repo's shipped default before claiming phase success.
  </behavior>
  <action>
    Determine the shipped default mode from `02.7-SPIKE-FINDINGS.md`, then run both modes explicitly:

    ```bash
    cd /home/mi/ws/gogo/roboclaws

    # Replace MODE_PRIMARY / MODE_COMPARE after reading 02.7-SPIKE-FINDINGS.md
    MODE_PRIMARY=stream
    MODE_COMPARE=terminal-body

    python examples/openclaw_nav_autonomous.py \
      --scene FloorPlan201 \
      --max-moves 30 \
      --wall-budget 180 \
      --transcript-mode "$MODE_PRIMARY" \
      --output-dir output/probes/02.7-primary

    python examples/openclaw_nav_autonomous.py \
      --scene FloorPlan201 \
      --max-moves 30 \
      --wall-budget 180 \
      --transcript-mode "$MODE_COMPARE" \
      --output-dir output/probes/02.7-compare
    ```

    For each run, record:

    - `run_result.json["terminated_by"]`
    - `run_result.json["transcript_source"]`
    - transcript message count
    - whether `trace.jsonl` contains `assistant_transcript`
    - whether `report.html` visibly renders transcript rows
    - first transcript timestamp, last transcript timestamp, and whether the sequence is monotonic
    - the nearest surrounding tool-log timestamps so the write-up can say whether transcript timing lines up with the replay

    Then write `.planning/phases/02.7-openclaw-intermediate-message-capture/02.7-LOCAL-PROBE-RESULTS.md`
    with:

    1. Date and host context
    2. The shipped default mode and why
    3. Primary run command + artifact path
    4. Compare run command + artifact path
    5. Observed transcript behavior for each mode
    6. Final shipped verdict:
       - `streaming-primary confirmed`
       - `terminal-body-primary confirmed`
       - or `Plan-01 verdict overturned` (with explicit reason)
    7. Known limitations, especially any mismatch between transcript timing and tool log timing

    Do not paste secrets. Use `$OPENCLAW_GATEWAY_TOKEN` literal in commands if you need to show env usage.

    If the final shipped verdict is `Plan-01 verdict overturned`, do these remediation steps before marking the plan done:

    1. Update `roboclaws/openclaw/bridge.py` so the module-level default transcript mode matches the validated winner.
    2. Append a short dated note to `02.7-SPIKE-FINDINGS.md` explaining that the earlier spike verdict was superseded by full autonomous validation and naming the new winner.
    3. Re-run:

       ```bash
       env -i PATH=".venv/bin:/usr/bin:/bin" HOME=$HOME .venv/bin/pytest \
         tests/test_bridge.py \
         tests/test_bridge_start_run.py \
         tests/test_openclaw_nav_autonomous.py \
         tests/test_render_autonomous_replay.py -q
       ```

    4. Re-run one primary validation pass with the corrected shipped default semantics. Prefer omitting `--transcript-mode` on that confirmation run so the artifact proves the default path, not just the override path.
  </action>
  <verify>
    <automated>test -f /home/mi/ws/gogo/roboclaws/.planning/phases/02.7-openclaw-intermediate-message-capture/02.7-LOCAL-PROBE-RESULTS.md && rg -n "Transcript source|output/probes/02.7-primary|output/probes/02.7-compare|confirmed|overturned" /home/mi/ws/gogo/roboclaws/.planning/phases/02.7-openclaw-intermediate-message-capture/02.7-LOCAL-PROBE-RESULTS.md</automated>
  </verify>
  <done>`02.7-LOCAL-PROBE-RESULTS.md` exists and records both primary and compare runs plus the final shipped verdict.</done>
</task>

<task type="auto">
  <name>Task 3: Update `docs/openclaw-local.md` with transcript expectations</name>
  <read_first>
    - docs/openclaw-local.md (the existing MCP tool surface and autonomous demo sections)
    - .planning/phases/02.7-openclaw-intermediate-message-capture/02.7-LOCAL-PROBE-RESULTS.md
    - examples/openclaw_nav_autonomous.py (final CLI surface)
  </read_first>
  <behavior>
    - `docs/openclaw-local.md` gains a targeted transcript subsection in the autonomous MCP area.
    - The docs tell operators where to find transcript data:
      - `trace.jsonl`
      - `run_result.json`
      - `report.html`
    - If `--transcript-mode` shipped, the docs mention it as a validation/debug aid.
    - The docs explain what `terminal-body` means: only the final body was captured, not true intermediate messages.
    - The docs link to `02.7-LOCAL-PROBE-RESULTS.md` for live evidence.
  </behavior>
  <action>
    Edit `docs/openclaw-local.md` near the existing Phase 2.6 autonomous MCP section. Add a new subsection titled:

    `### Transcript capture (Phase 2.7)`

    It must include:

    - the fact that `report.html` now has a transcript section when transcript
      data exists
    - `trace.jsonl` contains additive `assistant_transcript` events
    - `run_result.json` exposes `transcript_source` and `transcript_messages`
    - if available, `--transcript-mode {stream|terminal-body}` can force the
      capture path for comparison runs
    - a short explanation of `terminal-body`: it preserves only the terminal
      visible body and should not be mistaken for true intermediate capture
    - a link to
      `.planning/phases/02.7-openclaw-intermediate-message-capture/02.7-LOCAL-PROBE-RESULTS.md`

    Keep the existing Phase 2.6 MCP architecture explanation intact. This is a
    small additive operator note, not a rewrite.
  </action>
  <verify>
    <automated>bash -c 'f=/home/mi/ws/gogo/roboclaws/docs/openclaw-local.md; rg -n "Transcript capture \\(Phase 2.7\\)|assistant_transcript|transcript_source|terminal-body|02.7-LOCAL-PROBE-RESULTS" "$f"'</automated>
  </verify>
  <done>`docs/openclaw-local.md` has a transcript subsection with artifact locations, mode explanation, and evidence link.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local probe evidence ↔ repo docs | The docs will describe whatever this local validation proves. If the write-up is sloppy, future operators get false expectations. |
| Local shell ↔ repo-tracked markdown | The validation run uses real bearer tokens and provider keys; they must not leak into tracked docs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.7-12 | Information Disclosure | Probe notes / docs | mitigate | Commands in markdown use env-var placeholders; do not paste raw Authorization headers or token values into tracked files. |
| T-02.7-13 | Repudiation | Final shipped verdict | mitigate | Record both the default-mode run and the compare-mode run, with artifact paths and dated commands, so the final verdict is evidence-backed. |
| T-02.7-14 | Tampering | Operator docs | mitigate | Limit the doc change to a focused transcript subsection linked to the live evidence file; do not rewrite the whole Phase 2.6 architecture section from memory. |

No `high`-severity threats; plan proceeds.
</threat_model>

<verification>
- `02.7-LOCAL-PROBE-RESULTS.md` exists and records both primary and compare runs.
- If the verdict was overturned, `roboclaws/openclaw/bridge.py` and `02.7-SPIKE-FINDINGS.md` were updated before phase close-out.
- `docs/openclaw-local.md` contains `Transcript capture (Phase 2.7)` plus `assistant_transcript`, `transcript_source`, `terminal-body`, and the local-results link.
</verification>

<success_criteria>
- The shipped transcript path is proven or falsified with real local evidence.
- Future operators can read one doc section and know what transcript artifacts to expect.
- The phase ends with an explicit evidence-backed verdict, not just code changes.
</success_criteria>
