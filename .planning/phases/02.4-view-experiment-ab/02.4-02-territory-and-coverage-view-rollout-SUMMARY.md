---
phase: 02.4-view-experiment-ab
plan: 02-territory-and-coverage-view-rollout
subsystem: games
tags: [views, territory, coverage, examples, rollout]

requires:
  - phase: 02.4-01
    provides: Shared view primitives and variant builder
provides:
  - examples/territory_game.py --views wired on both direct-VLM and OpenClaw paths
  - examples/coverage_game.py --views wired on both direct-VLM and OpenClaw paths
  - Shared prompt-image assembly across the two multi-agent entry points
affects: [02.4-03-nvidia-provider-and-harness, 02.4-04-analysis-local-dev-and-writeup]

tech-stack:
  added: []
  patterns:
    - "Examples own prompt-image assembly; game-rule modules remain untouched"
    - "Shared helpers provide both ndarray prompt-image lists and encoded image-label payloads, keeping direct-VLM and OpenClaw paths aligned"
    - "Replay and final-report overhead frames mirror the selected variant rather than always showing the photo overhead"

key-files:
  created: []
  modified:
    - "examples/territory_game.py - --views CLI, shared-builder wiring, structured replay/final artifacts"
    - "examples/coverage_game.py - --views CLI, shared-builder wiring, structured replay/final artifacts"
    - "tests/test_views.py - extended example-level dispatch coverage"

key-decisions:
  - "Territory and coverage rules were left unchanged; this plan only changed what images are shown to the model and what artifacts mirror that choice."
  - "OpenClaw and direct-VLM paths both consume the same variant-selection surface, preventing the experiment harness from comparing different contracts by accident."
  - "Structured final maps are rendered from existing game state (`_agent_cells`, `_covered`) instead of introducing new persistence just for visualization."

patterns-established:
  - "Example drivers are the correct seam for Phase-2.4 view work; game semantics stay inside roboclaws/games/*"
  - "image_labels_for_variant(...) and build_prompt_images(...) define the one true variant contract for both provider families"

requirements-completed: [A-04]

duration: "same cloud session"
completed: 2026-04-21
---

# Phase 02.4 Plan 02: Territory And Coverage View Rollout Summary

**Rolled the new view system through the two multi-agent game entry points without changing scoring, blocking, or coverage semantics.**

## Accomplishments

- Added `--views` to `examples/territory_game.py` and `examples/coverage_game.py`.
- Replaced per-step prompt-image assembly in both drivers with the shared `roboclaws/core/views.py` helpers.
- Kept both provider paths aligned:
  - direct-VLM callers use encoded images from the shared helpers
  - OpenClaw callers reuse the same variant-specific image-label contract
- Updated replay and final-report artifacts so non-baseline runs visibly show the structured map variant they used.

## Verification

- `pytest -q tests/test_views.py` - passed
- `python examples/territory_game.py --help | rg -- --views` - passed
- `python examples/coverage_game.py --help | rg -- --views` - passed
- Existing example tests for territory and coverage continued to pass after the rollout

## Notes

- The returned run summaries now expose `total_steps` for both games and `coverage_pct` for coverage so the experiment harness can log a consistent per-run result row.
- This plan stayed strictly on the example seam. No rule changes were made inside `roboclaws/games/territory.py` or `roboclaws/games/coverage.py`.

## Task Commits

No per-task commits were created in this cloud session. The implementation is present in the current working tree and covered by the targeted checks above.

---
*Phase: 02.4-view-experiment-ab*
*Plan: 02-territory-and-coverage-view-rollout*
*Completed: 2026-04-21*
