---
phase: 02.4
plan: 03
slug: nvidia-provider-and-harness
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - roboclaws/core/vlm.py
  - examples/view_experiment.py
  - tests/test_nvidia_provider.py
  - tests/test_view_experiment.py
autonomous: true
requirements_addressed: [A-03, A-04]

must_haves:
  truths:
    - "roboclaws/core/vlm.py exposes NvidiaProvider plus create_provider aliases for the chosen NVIDIA model family."
    - "NvidiaProvider follows the existing structured-output contract and cost-accounting behavior without crashing on missing published prices."
    - "examples/view_experiment.py sweeps variants x seeds x scenes x games, appends JSONL rows, and enforces a cumulative --max-usd cap."
    - "Harness failures write status=error rows and continue instead of aborting the whole sweep."
    - "Mocked tests cover provider request shape and a 1x1x1x1 harness smoke."
---

<objective>
Add the second provider arm and the reproducible experiment harness that will consume the view variants built in the first two plans.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.4-view-experiment-ab/02.4-CONTEXT.md
@PLAN.md
@roboclaws/core/vlm.py
@examples/openclaw_demo.py
@examples/territory_game.py
@examples/coverage_game.py
</context>

<tasks>

<task>
<objective>
Implement `NvidiaProvider` in `roboclaws/core/vlm.py`.
</objective>

<action>
Mirror the OpenAI-compatible provider shape:
- `base_url="https://integrate.api.nvidia.com/v1"`
- `api_key=os.environ["NVIDIA_API_KEY"]`
- structured output via the same instructor flow
- model aliases for the chosen curated NVIDIA model(s)
- non-crashing cost handling even when a model price is unknown

Keep the live upstream probe out of cloud execution; that is part of `02.4-04`.
</action>

<verify>
`pytest -q tests/test_nvidia_provider.py`
</verify>
</task>

<task>
<objective>
Build the experiment harness.
</objective>

<action>
Create `examples/view_experiment.py` with:
- cross-product CLI for variants, seeds, scenes, games, model, agents
- per-run JSONL append
- replay directory output
- cumulative wallet gate via `--max-usd`
- error-row continuation semantics

The harness should call the existing example/game entry points instead of duplicating game logic.
</action>

<verify>
`pytest -q tests/test_view_experiment.py`
`python examples/view_experiment.py --help`
</verify>
</task>

</tasks>

## Verification

```bash
pytest -q tests/test_nvidia_provider.py tests/test_view_experiment.py
python examples/view_experiment.py --help
```
