---
phase: 02.4-view-experiment-ab
plan: 03-nvidia-provider-and-harness
subsystem: providers-and-experiments
tags: [nvidia, vlm, experiment-harness, jsonl, wallet-gate]

requires:
  - phase: 02.4-01
    provides: Shared view-variant contract and prompt-image builder
  - phase: 02.4-02
    provides: Territory and coverage example entry points with --views support
provides:
  - NvidiaProvider plus create_provider aliases in roboclaws/core/vlm.py
  - examples/view_experiment.py harness for variants x seeds x scenes x games sweeps
  - JSONL append-only result rows with wallet gating and error-row continuation
affects: [02.4-04-analysis-local-dev-and-writeup]

tech-stack:
  added: []
  patterns:
    - "NVIDIA uses the existing OpenAI-compatible provider shape with a different base_url and API key"
    - "Experiment harness calls the existing example/game entry points instead of duplicating gameplay logic"
    - "Harness failures append status=error rows and continue so overnight sweeps remain analyzable"

key-files:
  created:
    - "examples/view_experiment.py - reproducible sweep harness with JSONL append semantics"
    - "tests/test_nvidia_provider.py - provider request-shape and alias coverage"
    - "tests/test_view_experiment.py - 1x1x1x1 harness smoke and failure-row coverage"
  modified:
    - "roboclaws/core/vlm.py - NvidiaProvider, aliases, and non-crashing cost handling for unknown prices"

key-decisions:
  - "Provider aliases include both a default NVIDIA arm and explicit model names so the local-dev sweep can choose either the workhorse or the confirm model without extra wiring."
  - "Unknown or unpublished NVIDIA pricing must not crash the run; cost accounting degrades to zero-valued placeholders until a stable published price table is adopted."
  - "The harness writes one JSONL row per attempted run, including failure rows, because post-hoc analysis is more valuable than fail-fast behavior for this phase."

patterns-established:
  - "Experiment harnesses should log status, spend, wallclock, steps, and provider status in a flat JSONL schema"
  - "Cloud-safe provider work stops at mocked request-shape coverage; live upstream probes are explicitly deferred to local-dev"

requirements-completed: [A-03, A-04]

duration: "same cloud session"
completed: 2026-04-21
---

# Phase 02.4 Plan 03: Nvidia Provider And Harness Summary

**Added the second provider arm and the reproducible experiment harness that Phase 2.4 needs before any local sweep can be trusted.**

## Accomplishments

- Implemented `NvidiaProvider` in `roboclaws/core/vlm.py` using NVIDIA's OpenAI-compatible endpoint shape and the existing structured-output flow.
- Added model aliases for the Phase-2.4 NVIDIA arm and made missing published prices non-fatal to cost accounting.
- Created `examples/view_experiment.py`, which sweeps variants, seeds, scenes, and games; appends JSONL rows; enforces a cumulative `--max-usd` cap; and records `status=error` rows instead of aborting the whole study.
- Kept the harness thin by calling the existing territory and coverage example entry points instead of re-implementing game logic.

## Verification

- `pytest -q tests/test_nvidia_provider.py tests/test_view_experiment.py` - passed
- `python examples/view_experiment.py --help` - passed
- Targeted `tests/test_vlm.py` continued to pass after adding the new provider arm

## Notes

- No live NVIDIA call was claimed from this cloud session. Real provider probing remains part of `02.4-04` on a local workstation with credentials.
- The harness currently targets the two multi-agent games (`territory`, `coverage`), which are the primary comparison surface for the A/B study.

## Task Commits

No per-task commits were created in this cloud session. The implementation is present in the current working tree and covered by the targeted checks above.

---
*Phase: 02.4-view-experiment-ab*
*Plan: 03-nvidia-provider-and-harness*
*Completed: 2026-04-21*
