---
phase: 02.4
plan: 02
slug: territory-and-coverage-view-rollout
type: execute
wave: 2
depends_on: [01]
files_modified:
  - examples/territory_game.py
  - examples/coverage_game.py
  - tests/test_views.py
autonomous: true
requirements_addressed: [A-04]

must_haves:
  truths:
    - "examples/territory_game.py accepts --views and uses the shared view builder on both direct-VLM and OpenClaw paths."
    - "examples/coverage_game.py accepts --views and uses the shared view builder on both direct-VLM and OpenClaw paths."
    - "The existing territory and coverage rules stay unchanged; this plan only changes prompt-image assembly and CLI wiring."
    - "Targeted tests cover image-count dispatch and example-level variant selection for the game examples."
---

<objective>
Roll the same view system from `02.4-01` through the two multi-agent game entry points without changing game rules or metrics semantics.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v1.98-phases/02.4-view-experiment-ab/02.4-CONTEXT.md
@.planning/milestones/v1.98-phases/02.4-view-experiment-ab/02.4-01-openclaw-demo-view-variants-PLAN.md
@examples/territory_game.py
@examples/coverage_game.py
@roboclaws/core/views.py
</context>

<tasks>

<task>
<objective>
Wire `examples/territory_game.py` to the shared builder and add `--views`.
</objective>

<action>
Replace the per-step prompt-image assembly with `build_prompt_images(...)`.
Preserve the current prompt-state payload, payload metrics, and provider error handling.
</action>

<verify>
`python examples/territory_game.py --help | rg -- --views`
</verify>
</task>

<task>
<objective>
Wire `examples/coverage_game.py` to the shared builder and add `--views`.
</objective>

<action>
Match the territory rollout pattern. Keep field-of-view coverage accounting unchanged; this plan is about prompt images, not semantics.
</action>

<verify>
`python examples/coverage_game.py --help | rg -- --views`
</verify>
</task>

<task>
<objective>
Extend test coverage for example-level dispatch.
</objective>

<action>
Add or extend tests so each variant returns the expected image count and both example drivers expose the new CLI flag.
</action>

<verify>
`pytest -q tests/test_views.py`
</verify>
</task>

</tasks>

## Verification

```bash
pytest -q tests/test_views.py
python examples/territory_game.py --help | rg -- --views
python examples/coverage_game.py --help | rg -- --views
```
