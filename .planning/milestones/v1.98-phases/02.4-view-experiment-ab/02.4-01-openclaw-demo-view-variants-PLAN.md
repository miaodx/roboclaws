---
phase: 02.4
plan: 01
slug: openclaw-demo-view-variants
type: execute
wave: 1
depends_on: []
files_modified:
  - roboclaws/core/visualizer.py
  - roboclaws/core/engine.py
  - roboclaws/core/views.py
  - examples/openclaw_demo.py
  - tests/test_visualizer_structured.py
  - tests/test_engine_chase_cam.py
  - tests/test_views.py
autonomous: true
requirements_addressed: [A-04]

must_haves:
  truths:
    - "roboclaws/core/visualizer.py exposes render_structured_map(...) that renders reachable vs unreachable cells, claimed/covered overlays, and heading-aware agent markers."
    - "roboclaws/core/engine.py exposes chase-cam helpers that can register, update, and read a third-person frame for a given agent."
    - "roboclaws/core/views.py exists and returns the correct ndarray prompt-image list for baseline, map-v2, and map-v2+chase."
    - "examples/openclaw_demo.py accepts --views {baseline,map-v2,map-v2+chase} and routes the active agent through build_prompt_images(...)."
    - "Targeted unit tests cover structured-map rendering, chase-cam math, and view-count dispatch."
---

<objective>
Land the shared view surface on the smallest real Phase-2.4 seam first: the push-model OpenClaw navigation demo in `examples/openclaw_demo.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v1.98-phases/02.4-view-experiment-ab/02.4-CONTEXT.md
@PLAN.md
@examples/openclaw_demo.py
@roboclaws/core/visualizer.py
@roboclaws/core/engine.py

<notes>
- Keep this plan on ndarray prompt images only. `OpenClawProvider` already accepts numpy frames; base64 encoding for direct-VLM callers comes later.
- Do not entangle this plan with territory or coverage game logic. Their rollout is `02.4-02`.
</notes>
</context>

<tasks>

<task>
<objective>
Add the structured-map renderer to `roboclaws/core/visualizer.py`.
</objective>

<action>
Implement `render_structured_map(...)` using world-grid inputs instead of a photorealistic base frame:
- dark for unreachable
- light for reachable/unclaimed
- solid per-agent colors for claimed cells
- covered-cell color for cooperative runs
- triangle marker pointing in the agent's yaw direction

Keep `render_overhead_map(...)` unchanged for the `baseline` variant.
</action>

<verify>
`pytest -q tests/test_visualizer_structured.py`
</verify>
</task>

<task>
<objective>
Add chase-cam helpers to `MultiAgentEngine`.
</objective>

<action>
Implement stable helper methods for:
- registering a chase camera for an agent
- updating the chase-cam pose from agent position + yaw
- reading the chase-cam frame

Use mocked-controller tests for the pose math; live rendering validation stays in the later local-dev plan.
</action>

<verify>
`pytest -q tests/test_engine_chase_cam.py`
</verify>
</task>

<task>
<objective>
Create `roboclaws/core/views.py` and wire `examples/openclaw_demo.py`.
</objective>

<action>
Add `build_prompt_images(...)` that returns:
- 2 ndarrays for `baseline`
- 2 ndarrays for `map-v2`
- 3 ndarrays for `map-v2+chase`

Then add `--views` to `examples/openclaw_demo.py` and swap the current `[active_state.frame, map_frame]` assembly for the shared builder.
</action>

<verify>
`pytest -q tests/test_views.py`
`python examples/openclaw_demo.py --help | rg -- --views`
</verify>
</task>

</tasks>

## Verification

Run the targeted cloud-safe checks:

```bash
pytest -q tests/test_visualizer_structured.py tests/test_engine_chase_cam.py tests/test_views.py
python examples/openclaw_demo.py --help | rg -- --views
```

Local smoke of the actual camera pose is intentionally deferred to `02.4-04`.
