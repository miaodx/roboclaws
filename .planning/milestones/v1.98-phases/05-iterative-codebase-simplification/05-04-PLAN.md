---
plan: "05-04"
title: "Simplify roboclaws/openclaw/transport.py (already-leaner pass)"
wave: 1
depends_on: []
files_modified:
  - roboclaws/openclaw/transport.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "transport.py passes /simplify review with no high-severity findings"
    - "pytest exits 0 after changes"
    - "ruff check and ruff format --check exit 0"
    - "net line count is <= 849 (current post-260423-q71 state)"
    - "no public function or class signatures changed"
  artifacts:
    - path: "roboclaws/openclaw/transport.py"
      provides: "Simplified OpenClaw HTTP transport layer"
  key_links:
    - from: "roboclaws/openclaw/transport.py"
      to: "roboclaws/openclaw/bridge.py"
      via: "import OpenClawTransport"
      pattern: "from roboclaws.openclaw.transport import\|from roboclaws.openclaw import.*transport"
---

<objective>
Simplify roboclaws/openclaw/transport.py (849 lines) — remove any remaining dead code and improve readability.
Tests green. No public API changes.

Note: transport.py was recently refactored in quick task 260423-q71 (stream mode removed, done_event dead param extracted, _SessionStoreReader extracted). It is already leaner than the other targets. This /simplify pass should be lighter; apply only findings that clearly improve the code without over-engineering.
Output: transport.py committed atomically with any genuine simplifications applied and tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task id="T1" type="auto">
  <title>Simplify roboclaws/openclaw/transport.py</title>
  <read_first>
    - roboclaws/openclaw/transport.py
    - CLAUDE.md
  </read_first>
  <action>
    1. Run /simplify on roboclaws/openclaw/transport.py
    2. Review findings conservatively (transport.py was already refactored in 260423-q71):
       - Apply only clear wins: remove dead code, consolidate obvious duplication
       - Skip suggestions that introduce new abstractions or restructure session flow
       - Improve names where unambiguous improvement is clear
       - Remove unused imports
    3. If /simplify returns no high-severity findings, make only the obvious low-risk
       improvements and commit; do not over-process a file already in good shape.
    4. Grep callers to verify no public signatures changed:
       grep -rn "from roboclaws.openclaw.transport import\|from roboclaws.openclaw import" roboclaws/ examples/ tests/ scripts/ --include="*.py"
    5. Run: pytest -x -q (full suite — no dedicated transport unit test file exists; bridge tests cover transport indirectly)
    6. Run: ruff check . && ruff format --check .
    7. Commit: git commit -m "refactor(transport): simplify pass"
  </action>
  <acceptance_criteria>
    - pytest exits 0 (full suite)
    - ruff check exits 0
    - ruff format --check exits 0
    - git log --oneline -1 contains "refactor(transport): simplify pass"
    - No public function or class signatures changed (grep on callers confirms)
    - net line count is <= 849
  </acceptance_criteria>
</task>

</tasks>

## Must Haves
- Tests green after this plan (pytest exits 0)
- Ruff clean (check + format)
- No public API regressions
- Net change is a reduction or neutral (no new complexity added)
- Conservative application: only apply clear wins; do not restructure an already-lean file

<verification>
pytest tests/test_bridge.py tests/test_bridge_start_run.py -x -q
pytest -x -q
ruff check .
ruff format --check .
</verification>

<success_criteria>
transport.py simplified (or confirmed already clean) and committed atomically. pytest green. Ruff clean. Line count <= 849.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.98-phases/05-iterative-codebase-simplification/05-04-SUMMARY.md`
</output>
