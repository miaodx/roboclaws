---
plan: "05-09"
title: "Simplify examples/coverage_game.py and examples/territory_game.py"
wave: 4
depends_on: ["05-07", "05-01", "05-05"]
files_modified:
  - examples/coverage_game.py
  - examples/territory_game.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "coverage_game.py and territory_game.py pass /simplify review with no high-severity findings"
    - "pytest exits 0 after changes"
    - "ruff check and ruff format --check exit 0"
    - "net line counts at or below originals: coverage_game.py<=572, territory_game.py<=507"
    - "no CLI argument signatures changed"
  artifacts:
    - path: "examples/coverage_game.py"
      provides: "Simplified cooperative coverage game example"
    - path: "examples/territory_game.py"
      provides: "Simplified territory control game example"
  key_links:
    - from: "examples/coverage_game.py"
      to: "tests/test_coverage_example.py"
      via: "import coverage_game module"
      pattern: "from examples.coverage_game import\|import coverage_game"
    - from: "examples/territory_game.py"
      to: "tests/test_territory_example.py"
      via: "import territory_game module"
      pattern: "from examples.territory_game import\|import territory_game"
---

<objective>
Simplify examples/coverage_game.py (572 lines) and examples/territory_game.py (507 lines) — remove dead code, consolidate shared patterns, improve readability.
Tests green. No CLI argument changes.

Purpose: These two game example scripts share similar arg-parsing, game-loop orchestration, and output-saving patterns. A /simplify pass will surface dead code paths from earlier game variants, verbose setup chains, and any boilerplate that can be expressed more clearly.
Output: Both files committed atomically (one commit for both) with tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task id="T1" type="auto">
  <title>Simplify examples/coverage_game.py and examples/territory_game.py</title>
  <read_first>
    - examples/coverage_game.py
    - examples/territory_game.py
    - tests/test_coverage_example.py
    - tests/test_territory_example.py
    - CLAUDE.md
  </read_first>
  <action>
    1. Run /simplify on examples/coverage_game.py
    2. Run /simplify on examples/territory_game.py
    3. Review findings for both and apply:
       - Remove dead code paths (unused flags, unreachable branches, commented-out game variants)
       - Consolidate duplicated arg-parsing or output-saving patterns within each file
         (do NOT extract a shared examples/ utility module — simplify within each file)
       - Simplify verbose game-loop setup or agent-initialization chains
       - Improve names where ambiguous
       - Remove unused imports
    4. Verify no CLI argument signatures changed (--help output must stay the same):
       python examples/coverage_game.py --help
       python examples/territory_game.py --help
    5. Run: pytest tests/test_coverage_example.py tests/test_territory_example.py -x -q
    6. Run: pytest -x -q (full suite)
    7. Run: ruff check . && ruff format --check .
    8. Commit: git commit -m "refactor(game-examples): simplify pass"
  </action>
  <acceptance_criteria>
    - pytest exits 0 (full suite)
    - ruff check exits 0
    - ruff format --check exits 0
    - git log --oneline -1 contains "refactor(game-examples): simplify pass"
    - --help output unchanged for both scripts
    - net line counts: coverage_game.py<=572, territory_game.py<=507
  </acceptance_criteria>
</task>

</tasks>

## Must Haves
- Tests green after this plan (pytest exits 0)
- Ruff clean (check + format)
- No CLI argument regressions (--help unchanged)
- Net change is a reduction or neutral (no new complexity added)
- Do NOT extract a shared examples/ utility module

<verification>
pytest tests/test_coverage_example.py tests/test_territory_example.py -x -q
pytest -x -q
ruff check .
ruff format --check .
</verification>

<success_criteria>
coverage_game.py and territory_game.py simplified and committed atomically (one commit). pytest green. Ruff clean. Line counts <= originals.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.98-phases/05-iterative-codebase-simplification/05-09-SUMMARY.md`
</output>
