---
plan: "05-01"
title: "Simplify roboclaws/core/visualizer.py"
wave: 1
depends_on: []
files_modified:
  - roboclaws/core/visualizer.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "visualizer.py passes /simplify review with no high-severity findings"
    - "pytest exits 0 after changes"
    - "ruff check and ruff format --check exit 0"
    - "net line count is <= 996 (original)"
    - "no public function or class signatures changed"
  artifacts:
    - path: "roboclaws/core/visualizer.py"
      provides: "Simplified overhead map generation, frame compositing, GIF output"
  key_links:
    - from: "roboclaws/core/visualizer.py"
      to: "tests/test_visualizer.py, tests/test_visualizer_soul_overlay.py, tests/test_visualizer_structured.py"
      via: "import Visualizer"
      pattern: "from roboclaws.core.visualizer import|from roboclaws.core import.*visualizer"
---

<objective>
Simplify roboclaws/core/visualizer.py (996 lines) — remove dead code, consolidate helpers, improve readability.
Tests green. No public API changes.

Purpose: visualizer.py is the largest file in the codebase. A /simplify pass will surface dead helpers, duplicate drawing logic, and over-engineered compositing paths that have accumulated across view-experiment phases.
Output: Leaner visualizer.py committed atomically with tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task id="T1" type="auto">
  <title>Simplify roboclaws/core/visualizer.py</title>
  <read_first>
    - roboclaws/core/visualizer.py
    - tests/test_visualizer.py
    - tests/test_visualizer_soul_overlay.py
    - tests/test_visualizer_structured.py
    - CLAUDE.md
  </read_first>
  <action>
    1. Run /simplify on roboclaws/core/visualizer.py
    2. Review findings and apply:
       - Remove any dead helper functions not called outside the module
       - Consolidate duplicated drawing/compositing logic
       - Simplify overly complex conditional branches in frame composition
       - Improve names where they are ambiguous (without changing public API)
       - Remove unused imports
    3. Grep callers to verify no public signatures changed:
       grep -rn "from roboclaws.core.visualizer import\|from roboclaws.core import" roboclaws/ examples/ tests/ scripts/ --include="*.py"
    4. Run: pytest tests/test_visualizer.py tests/test_visualizer_soul_overlay.py tests/test_visualizer_structured.py -x -q
    5. Run: pytest -x -q (full suite)
    6. Run: ruff check . && ruff format --check .
    7. Commit: git commit -m "refactor(visualizer): simplify pass"
  </action>
  <acceptance_criteria>
    - pytest exits 0 (full suite)
    - ruff check exits 0
    - ruff format --check exits 0
    - git log --oneline -1 contains "refactor(visualizer): simplify pass"
    - No public function or class signatures changed (grep on callers confirms)
    - net line count is <= 996
  </acceptance_criteria>
</task>

</tasks>

## Must Haves
- Tests green after this plan (pytest exits 0)
- Ruff clean (check + format)
- No public API regressions
- Net change is a reduction or neutral (no new complexity added)

<verification>
pytest tests/test_visualizer.py tests/test_visualizer_soul_overlay.py tests/test_visualizer_structured.py -x -q
pytest -x -q
ruff check .
ruff format --check .
</verification>

<success_criteria>
visualizer.py simplified and committed atomically. pytest green. Ruff clean. Line count <= 996.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.98-phases/05-iterative-codebase-simplification/05-01-SUMMARY.md`
</output>
