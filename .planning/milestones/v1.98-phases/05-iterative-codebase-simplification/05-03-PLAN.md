---
plan: "05-03"
title: "Simplify roboclaws/core/reporter.py"
wave: 1
depends_on: []
files_modified:
  - roboclaws/core/reporter.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "reporter.py passes /simplify review with no high-severity findings"
    - "pytest exits 0 after changes"
    - "ruff check and ruff format --check exit 0"
    - "net line count is <= 856 (original)"
    - "no public function or class signatures changed"
  artifacts:
    - path: "roboclaws/core/reporter.py"
      provides: "Simplified HTML report generation"
  key_links:
    - from: "roboclaws/core/reporter.py"
      to: "tests/test_reporter.py"
      via: "import reporter"
      pattern: "from roboclaws.core.reporter import\|from roboclaws.core import.*reporter"
---

<objective>
Simplify roboclaws/core/reporter.py (856 lines) — remove dead code, consolidate HTML-building helpers, improve readability.
Tests green. No public API changes.

Purpose: reporter.py was partially refactored in quick task 260423-q71 (build_run_section split into helper methods). A full /simplify pass will surface remaining duplication in HTML template strings, redundant helper chains, and any builder methods that could be expressed more concisely.
Output: Leaner reporter.py committed atomically with tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task id="T1" type="auto">
  <title>Simplify roboclaws/core/reporter.py</title>
  <read_first>
    - roboclaws/core/reporter.py
    - tests/test_reporter.py
    - CLAUDE.md
  </read_first>
  <action>
    1. Run /simplify on roboclaws/core/reporter.py
    2. Review findings and apply:
       - Consolidate duplicated HTML-building or template-string patterns
       - Remove any builder helper methods that are trivially wrappable or inlineable
       - Simplify overly nested HTML-generation logic
       - Improve names where ambiguous (without changing public API)
       - Remove unused imports
    3. Note: The four private helpers extracted in 260423-q71 (_build_summary_bar,
       _collect_frame_data, _build_viewer_html, _build_vlm_log_html) are already good
       extractions — do not re-merge them; build on that structure.
    4. Grep callers to verify no public signatures changed:
       grep -rn "from roboclaws.core.reporter import\|from roboclaws.core import" roboclaws/ examples/ tests/ scripts/ --include="*.py"
    5. Run: pytest tests/test_reporter.py -x -q
    6. Run: pytest -x -q (full suite)
    7. Run: ruff check . && ruff format --check .
    8. Commit: git commit -m "refactor(reporter): simplify pass"
  </action>
  <acceptance_criteria>
    - pytest exits 0 (full suite)
    - ruff check exits 0
    - ruff format --check exits 0
    - git log --oneline -1 contains "refactor(reporter): simplify pass"
    - No public function or class signatures changed (grep on callers confirms)
    - net line count is <= 856
  </acceptance_criteria>
</task>

</tasks>

## Must Haves
- Tests green after this plan (pytest exits 0)
- Ruff clean (check + format)
- No public API regressions
- Net change is a reduction or neutral (no new complexity added)

<verification>
pytest tests/test_reporter.py -x -q
pytest -x -q
ruff check .
ruff format --check .
</verification>

<success_criteria>
reporter.py simplified and committed atomically. pytest green. Ruff clean. Line count <= 856.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.98-phases/05-iterative-codebase-simplification/05-03-SUMMARY.md`
</output>
