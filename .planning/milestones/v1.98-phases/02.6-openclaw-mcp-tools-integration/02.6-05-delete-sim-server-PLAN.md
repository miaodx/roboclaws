---
phase: 02.6
plan: 05
slug: delete-sim-server
type: execute
wave: 3
depends_on: ["02.6-04"]
files_modified:
  - roboclaws/openclaw/sim_server.py
  - tests/test_sim_server.py
  - scripts/openclaw-bootstrap.sh
autonomous: true
requirements_addressed: [A-06]

must_haves:
  truths:
    - "roboclaws/openclaw/sim_server.py does not exist."
    - "tests/test_sim_server.py does not exist."
    - "No source file under roboclaws/, examples/, or tests/ imports from roboclaws.openclaw.sim_server."
    - "scripts/openclaw-bootstrap.sh no longer contains the `-e SIM_SERVER_URL=...` argument in the Gateway `docker run -d` block (the deprecated placeholder left by plan 02 is now deleted)."
    - "The full test suite (pytest) still passes after deletion."
  artifacts:
    - path: "roboclaws/openclaw/sim_server.py"
      provides: "DELETED — superseded by mcp_server.py in plan 01"
      contains: "<file must not exist>"
    - path: "tests/test_sim_server.py"
      provides: "DELETED — superseded by tests/test_openclaw_mcp_server.py in plan 01"
      contains: "<file must not exist>"
    - path: "scripts/openclaw-bootstrap.sh"
      provides: "The -e SIM_SERVER_URL=... line has been removed from the Gateway docker run args; the deprecation-warning log line at bootstrap start may stay (harmless for operators with stale shells) or be removed — executor's call."
      contains: "ROBOCLAWS_MCP_URL"
  key_links:
    - from: "(nothing in the codebase)"
      to: "roboclaws.openclaw.sim_server"
      via: "all prior imports were rewired to mcp_server in plan 04; this plan removes the module"
      pattern: "from roboclaws\\.openclaw\\.sim_server"
    - from: "scripts/openclaw-bootstrap.sh (Gateway docker run -d block)"
      to: "(no longer exports SIM_SERVER_URL)"
      via: "Plan 02 left it as a no-op placeholder; plan 05 removes it entirely"
      pattern: "SIM_SERVER_URL"
---

<objective>
Delete the superseded `roboclaws/openclaw/sim_server.py` module and its test file, and close out the `SIM_SERVER_URL` deprecation by removing the no-op placeholder that plan 02 left in `scripts/openclaw-bootstrap.sh`. Per D-05 in CONTEXT.md, the default is delete; plans 01-04 have fully replaced the HTTP-over-curl contract with the MCP tool surface, and leaving sim_server.py as dead code invites drift — a future hand might see it and accidentally reintroduce the pattern Phase 2.5 proved wrong.

Purpose: Close the Phase 2.5 artifact cleanly. The module delete is the main act; the bootstrap cleanup is the tail end of the SIM_SERVER_URL deprecation cycle that plan 02 started.

Output: Two file deletions, one bootstrap edit that removes the dead `-e SIM_SERVER_URL=...` argument, a grep-verified absence of imports, and a green pytest run to prove no regression.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v1.98-phases/02.6-openclaw-mcp-tools-integration/02.6-CONTEXT.md
@roboclaws/openclaw/sim_server.py
@tests/test_sim_server.py
@scripts/openclaw-bootstrap.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify no live importers remain, delete sim_server.py + test_sim_server.py, and remove the `-e SIM_SERVER_URL=...` line from the Gateway docker run args</name>
  <read_first>
    - .planning/milestones/v1.98-phases/02.6-openclaw-mcp-tools-integration/02.6-CONTEXT.md (D-05 — default is delete)
    - roboclaws/openclaw/sim_server.py (confirm you are deleting the right file — the 12,781-byte HTTP sim-server module)
    - tests/test_sim_server.py (confirm this tests ONLY sim_server)
    - examples/openclaw_nav_autonomous.py (post-plan-04; confirm no remaining import of sim_server)
    - scripts/openclaw-bootstrap.sh (post-plan-02; locate the no-op `-e "SIM_SERVER_URL=${SIM_SERVER_URL}"` argument in the Gateway `docker run -d` block, approximately line 549-570)
  </read_first>
  <behavior>
    - `git ls-files | grep "openclaw/sim_server.py"` returns no matches after deletion.
    - `grep -rn "from roboclaws.openclaw.sim_server\|import sim_server\|openclaw\.sim_server" roboclaws/ examples/ tests/ scripts/ 2>/dev/null` returns no matches.
    - `grep -c "\\-e \"SIM_SERVER_URL=" scripts/openclaw-bootstrap.sh` returns 0 (the dead docker-run argument is gone).
    - The deprecation fallback in plan 02 Task 1 step 1 (the `ROBOCLAWS_MCP_URL` default resolver that accepts `SIM_SERVER_URL` and emits the `WARN: SIM_SERVER_URL is deprecated` log line) stays — it is harmless and helps operators whose shells still export the legacy var.
    - `pytest -q` (full suite) passes with the sim_server tests subtracted from the pre-plan count but no new failures.
  </behavior>
  <action>
    Execute in this order:

    1. Run a dependency scan FIRST to prove nothing still depends on sim_server:
       ```bash
       grep -rn "from roboclaws.openclaw.sim_server\|import sim_server\|openclaw\\.sim_server\|SimHTTPServer" \
         roboclaws/ examples/ tests/ scripts/ 2>/dev/null
       ```
       Expected output: zero matches. If there ARE matches, STOP — plan 04 did not fully land; fix there first.

    2. Delete the two files via `git rm`:
       ```bash
       git rm roboclaws/openclaw/sim_server.py tests/test_sim_server.py
       ```

    3. Remove the dead `-e "SIM_SERVER_URL=${SIM_SERVER_URL}"` argument from the Gateway `docker run -d` block in `scripts/openclaw-bootstrap.sh` (plan 02 left it as a no-op placeholder; plan 05 deletes it as sim_server itself is deleted). The line lives near line 549-570 in the mount_args block. After editing, run `bash -n scripts/openclaw-bootstrap.sh` to confirm the script still parses. Do NOT touch the plan-02 fallback block near line 89-102 that emits the deprecation warning — that is the graceful-degrade path for operators with stale shells and stays.

    4. Run the full test suite to confirm no regression:
       ```bash
       pytest -q
       ```
       Expected: all tests pass.

    5. Confirm deletion + script edit:
       ```bash
       test ! -e roboclaws/openclaw/sim_server.py && test ! -e tests/test_sim_server.py && echo deleted
       grep -c "\\-e \"SIM_SERVER_URL=" scripts/openclaw-bootstrap.sh    # expected: 0
       bash -n scripts/openclaw-bootstrap.sh                              # expected: exit 0
       ```

    DO NOT delete anything else. DO NOT touch `roboclaws/openclaw/__init__.py`. DO NOT remove the `diagnostics.py` module. DO NOT remove the plan-02 `WARN: SIM_SERVER_URL is deprecated` log line — that is user-facing guidance and is cheap to keep.
  </action>
  <verify>
    <automated>cd /home/mi/ws/gogo/roboclaws && test ! -e roboclaws/openclaw/sim_server.py && test ! -e tests/test_sim_server.py && test "$(grep -c '\\-e \"SIM_SERVER_URL=' scripts/openclaw-bootstrap.sh)" = "0" && bash -n scripts/openclaw-bootstrap.sh && pytest -q 2>&1 | tail -3</automated>
  </verify>
  <done>Both sim_server files do not exist on disk. `grep -rn "from roboclaws.openclaw.sim_server" roboclaws/ examples/ tests/ scripts/` returns 0 matches. `grep -c "\\-e \"SIM_SERVER_URL=" scripts/openclaw-bootstrap.sh` returns 0. `bash -n scripts/openclaw-bootstrap.sh` exits 0. `pytest -q` exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Code surface area | Every module that exists is a thing a future developer might call into. Deleting a superseded module shrinks the attack/misuse surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.6-16 | Tampering | A future edit reintroduces `sim_server.py` patterns (HTTP over curl via exec) | mitigate (by removal) | With the file deleted, any attempt to resurrect the pattern requires writing a new module with the same anti-pattern — far more visible in review than editing a file that already exists. |
| T-02.6-17 | Information Disclosure | sim_server.py exposed an HTTP server on 0.0.0.0:18788 by default (vs. MCP server's 127.0.0.1 default in plan 01) | mitigate (by removal) | Deleting the module removes the LAN-exposed listener from the codebase entirely. |
| T-02.6-18 | Denial of Service | Dependency scan misses an import, pytest breaks | mitigate | Task step 1 runs a recursive grep across 4 directories before deletion; step 4 runs the full pytest suite after. Two independent gates. |
| T-02.6-26 | Repudiation | Stale `-e SIM_SERVER_URL=` in bootstrap after sim_server is deleted would silently pass garbage into the container env | mitigate (by removal) | Task step 3 removes the docker-run argument; verify step grep asserts its absence. |

No `high`-severity threats; plan proceeds.
</threat_model>

<verification>
- `test ! -e roboclaws/openclaw/sim_server.py` exits 0.
- `test ! -e tests/test_sim_server.py` exits 0.
- `grep -rn "from roboclaws.openclaw.sim_server\|SimHTTPServer" roboclaws/ examples/ tests/ scripts/` returns 0 matches.
- `grep -c "\\-e \"SIM_SERVER_URL=" scripts/openclaw-bootstrap.sh` returns 0.
- `bash -n scripts/openclaw-bootstrap.sh` exits 0.
- `pytest -q` exits 0.
</verification>

<success_criteria>
- Both sim_server files deleted from the working tree and staged for the phase commit.
- Zero remaining imports or references to `sim_server` anywhere under `roboclaws/`, `examples/`, `tests/`, `scripts/`.
- The `-e "SIM_SERVER_URL=..."` line in the Gateway `docker run -d` block of `scripts/openclaw-bootstrap.sh` is deleted; the plan-02 deprecation-warning fallback stays.
- Full pytest suite green.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.98-phases/02.6-openclaw-mcp-tools-integration/02.6-05-SUMMARY.md`.
</output>
