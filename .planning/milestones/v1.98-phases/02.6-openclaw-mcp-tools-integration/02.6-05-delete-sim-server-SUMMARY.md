---
phase: 02.6-openclaw-mcp-tools-integration
plan: 05
subsystem: infra
tags: [cleanup, deletion, mcp, openclaw, sim_server, bootstrap]

requires:
  - phase: 02.6-openclaw-mcp-tools-integration
    provides: "Plans 01-04 delivered the MCP tool surface (mcp_server.py, bootstrap seeding, slimmed SKILL.md, rewired example) that fully replaces the HTTP sim_server"
provides:
  - "Clean deletion of the superseded HTTP sim_server module and its test suite"
  - "Removal of the dead `-e SIM_SERVER_URL=...` docker-run argument from the Gateway bootstrap"
  - "Proof (via recursive grep + full pytest) that no live code path depends on sim_server"
affects: [02.6-06-live-probe-gate, 02.6-07-docs-update, future-phase-3]

tech-stack:
  added: []
  patterns:
    - "Pure-deletion plan pattern: dependency scan → delete → rebuild gate. No replacement work bundled."

key-files:
  created: []
  modified:
    - "scripts/openclaw-bootstrap.sh (removed dead -e SIM_SERVER_URL docker-run arg; plan-02 fallback kept)"
  deleted:
    - "roboclaws/openclaw/sim_server.py (348 LOC HTTP sim-server module)"
    - "tests/test_sim_server.py (283 LOC test suite)"

key-decisions:
  - "Kept the plan-02 SIM_SERVER_URL → ROBOCLAWS_MCP_URL deprecation fallback (lines 106-116 in bootstrap) intact — graceful-degrade for operators with stale shells. Only the docker-run placeholder arg (line 620 pre-edit) was removed."
  - "Kept historical doc-comments referencing sim_server.py in mcp_server.py, example, tests, and trace_schema_reference.json — these document schema continuity (run_result_json['sim_server_metrics'] key frozen; trace schema frozen per D-05). Scrubbing them would erase the evolution history."
  - "Pre-existing ruff warnings in unrelated files (scripts/diagnose_openclaw_latency.py, tests/test_visualizer_soul_overlay.py, roboclaws/core/visualizer.py, etc.) left alone per scope-boundary — logged to deferred-items.md for a future `chore: ruff cleanup` PR."

patterns-established:
  - "Pure-deletion plan: dependency-scan before the delete, full-suite test gate after, zero replacement work bundled. Works when upstream plans (01-04 here) have fully migrated the callers."
  - "Deferred-items routing: pre-existing lint errors in untouched files go to phase-level deferred-items.md, not plan-level deviations."

requirements-completed: [A-06]

duration: 7min
completed: 2026-04-21
---

# Phase 02.6 Plan 05: delete-sim-server Summary

**Deleted the 631-LOC Phase 2.5 HTTP sim_server module (and its test suite) plus the dead `-e SIM_SERVER_URL=...` docker-run arg from the Gateway bootstrap; zero live importers, full pytest still green.**

## Performance

- **Duration:** 7 min
- **Started:** 2026-04-21T10:36:01Z
- **Completed:** 2026-04-21T10:43:11Z
- **Tasks:** 1
- **Files modified:** 1 (scripts/openclaw-bootstrap.sh)
- **Files deleted:** 2 (sim_server.py, test_sim_server.py)
- **Net LOC:** -635

## Accomplishments

- **Zero-dependency delete proven before execution.** The prescribed recursive grep
  (`from roboclaws.openclaw.sim_server | import sim_server | openclaw\.sim_server | SimHTTPServer`)
  across `roboclaws/ examples/ tests/ scripts/` returned only matches inside the
  two files being deleted. Plan 04's example-rewire landed completely.
- **sim_server.py and test_sim_server.py removed** via `git rm` (staged, then
  committed atomically).
- **Dead `-e "SIM_SERVER_URL=${SIM_SERVER_URL}"` docker-run arg removed** from the
  Gateway container launch in `scripts/openclaw-bootstrap.sh`. Bootstrap passes
  `bash -n` syntax check; grep for the dead arg returns 0.
- **Full pytest suite still green** — 475 passed, 1 skipped (unchanged counts net
  of the subtracted `test_sim_server.py` suite).

## Task Commits

Single-task plan; one atomic commit:

1. **Task 1: Delete sim_server + test_sim_server, remove dead bootstrap arg** —
   `3a8f6c3` (`refactor(phase-02.6-05): delete superseded HTTP sim_server`)

**Plan metadata commit:** will be the commit carrying this SUMMARY.md + STATE.md.

## Files Created/Modified

### Deleted

- `roboclaws/openclaw/sim_server.py` (348 LOC) — Phase 2.5 HTTP sim-server class
  `SimHTTPServer`. Fully superseded by `roboclaws/openclaw/mcp_server.py`
  (plan 01) which serves the same three tools (observe, move, done) over MCP
  streamable-http instead of plain HTTP-over-curl.
- `tests/test_sim_server.py` (283 LOC) — Mocked-engine HTTP test suite. Its
  coverage carries forward into `tests/test_openclaw_mcp_server.py` (plan 01),
  which tests the same tool semantics against the MCP surface plus the frozen
  trace-schema contract from `tests/fixtures/trace_schema_reference.json`.

### Modified

- `scripts/openclaw-bootstrap.sh` — Removed the 4-line block (`-e SIM_SERVER_URL=`
  argument + surrounding 3-line explanatory comment) from the Gateway
  `docker run -d` mount_args. The plan-02 `SIM_SERVER_URL → ROBOCLAWS_MCP_URL`
  deprecation-warning fallback (lines 106-116) is preserved per the plan
  directive — that's the graceful-degrade path for operators whose shells still
  export the legacy var.

### Created

- `.planning/milestones/v1.98-phases/02.6-openclaw-mcp-tools-integration/deferred-items.md` —
  Records pre-existing ruff violations in unrelated files as out-of-scope for
  this plan, with provenance evidence and a suggested follow-up cleanup PR.

## Verification Evidence

All five gates from the plan's `<verification>` block passed:

| Gate | Command | Result |
|------|---------|--------|
| sim_server.py deleted | `test ! -e roboclaws/openclaw/sim_server.py` | exit 0 |
| test_sim_server.py deleted | `test ! -e tests/test_sim_server.py` | exit 0 |
| No live refs | `grep -rn "from roboclaws.openclaw.sim_server\|SimHTTPServer" roboclaws/ examples/ tests/ scripts/` | 0 matches |
| Dead docker-run arg gone | `grep -c '\-e "SIM_SERVER_URL=' scripts/openclaw-bootstrap.sh` | 0 |
| Bootstrap still parses | `bash -n scripts/openclaw-bootstrap.sh` | exit 0 |
| Full suite green | `env -i ... .venv/bin/pytest` | 475 passed, 1 skipped |

## Decisions Made

- **Kept the plan-02 fallback block** (bootstrap lines 106-116). Plan 05's action
  explicitly said "Do NOT touch the plan-02 fallback block... that is the
  graceful-degrade path for operators with stale shells and stays." Followed.
- **Kept historical doc-comments** referencing sim_server.py in
  `roboclaws/openclaw/mcp_server.py` docstring, `examples/openclaw_nav_autonomous.py`
  (`sim_server_metrics` key comment), `tests/test_openclaw_mcp_server.py` (schema
  comment), `tests/test_openclaw_nav_autonomous.py` (frozen-key comment), and
  `tests/fixtures/trace_schema_reference.json` (source-pointer metadata). These
  document the schema-continuity contract (D-05: keyset carried forward verbatim
  from sim_server for renderer compat) — scrubbing them would erase the evolution
  history. The plan's dependency-scan pattern deliberately scoped to
  `from/import`-style references and the `SimHTTPServer` symbol, not all string
  matches of "sim_server", which aligns with this decision.
- **Deferred pre-existing ruff violations** to
  `deferred-items.md` rather than fixing them in this plan. Per scope-boundary:
  "only auto-fix issues DIRECTLY caused by the current task's changes."

## Deviations from Plan

None — plan executed exactly as written. The `<action>` block's five numbered
steps were all executed in order with no surprises: dependency scan returned 0
external importers (as expected post-plan-04), `git rm` succeeded, the
`-e "SIM_SERVER_URL=..."` line + its 3-line comment were removed from the
bootstrap's mount_args, `bash -n` and `pytest` both exited 0.

## Issues Encountered

- **Ruff noise in unrelated files.** The user prompt added `ruff check` and
  `ruff format --check` to the success criteria. Running them surfaced 5 check
  errors + 6 format diffs, all in files *not* modified by this plan
  (`scripts/diagnose_openclaw_latency.py`, `tests/test_visualizer_soul_overlay.py`,
  `roboclaws/core/visualizer.py`, etc. — all introduced in phases predating
  02.6). Per executor scope-boundary, these were logged to `deferred-items.md`
  rather than fixed here. Ruff is clean on the files this plan touched
  (`roboclaws/openclaw/` and the Python files under it; the bootstrap is a shell
  script and ruff doesn't check shell).

## User Setup Required

None — pure code deletion.

## Next Phase Readiness

- **Plan 06 (live-probe-gate)** unblocked: the MCP surface is the only tool path
  left; no risk of a local-dev run accidentally routing through a stale HTTP
  sim_server.
- **Plan 07 (docs-update)** will pick up the `docs/openclaw-local.md` section
  (lines 239-276) that still describes the sim_server-era operator recipe. That
  migration was deliberately deferred to plan 07 — plan 05's scope was code
  deletion only.
- **Deferred items** recorded in
  `.planning/milestones/v1.98-phases/02.6-openclaw-mcp-tools-integration/deferred-items.md`:
  - Pre-existing ruff violations in 5+ files (not introduced by any Phase 02.6
    plan; separate cleanup PR recommended).

## Self-Check: PASSED

All 8 gates verified post-commit on 2026-04-21T10:43Z:

- OK: `roboclaws/openclaw/sim_server.py` does not exist
- OK: `tests/test_sim_server.py` does not exist
- OK: `02.6-05-delete-sim-server-SUMMARY.md` exists
- OK: `deferred-items.md` exists
- OK: task commit `3a8f6c3` present in git log
- OK: `bash -n scripts/openclaw-bootstrap.sh` passes
- OK: `grep -c '\-e "SIM_SERVER_URL=' scripts/openclaw-bootstrap.sh` returns 0
- OK: `grep -rn "from roboclaws.openclaw.sim_server|import sim_server|openclaw\.sim_server|SimHTTPServer" roboclaws/ examples/ tests/ scripts/` returns 0 matches

---

*Phase: 02.6-openclaw-mcp-tools-integration*
*Completed: 2026-04-21*
